// Notebook host placeholder
// Upstream sync introduced an `EditParams` schema for a notebook tool that
// depends on `effect`'s Schema/Path/Revision/Index primitives that are not
// available in this codebase yet. Tracked for future implementation; kept as
// a stub so the build stays green.
export {};
