# Architecture Review -- 2026-09-07 (scheduled, master)

- Trigger: `schedule` on `refs/heads/master`
- HEAD: `bccf593e` (`chore(ci): factory consulting run [agent]`)
- Previous reviews: `001-establish-adr-process.md`, `REVIEW-2026-06-04.md`,
  `REVIEW-2026-06-04T12-followup.md`, `REVIEW-2026-06-15.md`,
  `REVIEW-2026-06-29.md`, `REVIEW-2026-07-06.md`, `REVIEW-2026-07-27.md`,
  `REVIEW-2026-08-17.md`, `REVIEW-2026-08-24.md`, `REVIEW-2026-08-31.md`

## Summary

Tenth scheduled architecture run. One week since `REVIEW-2026-08-31`.
Merge traffic dominated by dependabot bumps (puppeteer, zod, mammoth,
tsx, `@typescript-eslint/eslint-plugin`, `@inquirer/prompts`), two
`feat(sync)` PRs (#1647 2026-09-04, #1657 2026-09-06), one apply-patch
ADD-mode feature (#1656), one agenticChat prompt-cache hardening
(#1651), plus the usual auto-generated documentation churn.

**Headline architectural news in this run:**

- **NO new top-level `src/<name>/` directory landed.** Second cycle
  running clean at directory level. Top-level count stable at **35**.
- **NO new sub-directory landed under `src/core/`, `src/tool/`,
  `src/providers/`, or any other layer.** Last cycle's F25/F26/F27
  sub-directory add rate (three in one week) did not repeat. Good.
- **ONE new zero-inbound file landed inside `src/providers/`:**
  `src/providers/gitlab.ts` (100 lines) via #1657 (2026-09-06 sync).
  New F29 (`warn`). No layering violation (SDK use is confined to the
  correct layer), but same F21 pattern -- upstream sync pre-lands
  modules with no local caller. Consumer scan is empty.
- **Rule 4 (F20) STILL not fixed.** `src/core/router.ts:12` still
  imports `c` from `../cli/utils/colors.js`. **Fourth consecutive
  cycle at `block`.** Longest-running unresolved rule-4 violation in
  this review series.
- **F17 unchanged.** All eight documented `src/tool/tools/*.ts` ->
  `src/agent/` and `src/tool/tools/*.ts` -> `src/core/` upward imports
  are still there byte-identical. Seventh review of the tool-layer
  carve-outs.
- **F18 unchanged.** `src/permission/index.ts:26` still imports
  `getAllToolNames` from `../tool/registry.js`. Eighth review.
- **Auto-delete deadlines from last cycle (F22, F23) EXPIRED with
  no action.** `REVIEW-2026-08-31` set today (`REVIEW-2026-09-07`) as
  the auto-delete date for `src/session/reminders.ts` +
  `ask-code-switch.txt` (F22) and `src/tool/grep.ts` +
  `src/tool/grep-signal-controls.ts` (F23). Both remain byte-identical
  and zero-inbound. F22 -> **fourth review at `warn`**; F23 -> **fourth
  review at `warn`**. Per last cycle's commitment ("do not defer
  further"), these ESCALATE to `block` this cycle with an explicit
  disposition (Recommendation 4).
- **F25 / F26 / F27 dispositions from last cycle:**
  - **F25** `src/core/session/*` -- 4 files, still zero-inbound
    externally. Two-cycle grace period elapses next cycle. Escalates
    to `block` on 2026-09-14 if no wire lands. Held at `warn` today.
  - **F26** `src/core/pty/termination.ts` -- still zero-inbound.
    Recommendation was to wire into bash-tool kill path OR delete
    this cycle. Neither happened. **Escalates to `block`** now;
    2026-09-14 deadline for disposition.
  - **F27** `src/core/kilocode/zero-id.ts` -- still zero-inbound.
    `src/core/kilocode/fff.ts` still wired only from
    `src/core/filesystem/watcher.ts:35` (unchanged). No ADR 006.
    F27 held at `warn` (subordinate to F24 / ADR 006).
- **F24 escalation clause TRIGGERED.** Last cycle:
  *"if there is no ADR OR rename by REVIEW-2026-09-07, this escalates
  to `block`."* No ADR 006 exists (`ls docs/adr/006* -> no 006`), no
  rename of `src/kilocode/` occurred. **F24 escalates to `block`
  this cycle** as promised.
- **F10 / F11 / F13 unchanged.** `src/core/billing.ts` (188 B, still
  zero-ref, sixth review). `src/core/catalog.ts` (4256 B now, still
  zero-ref, sixth review, still superseded by
  `src/providers/modelCatalog.ts`). `src/session/data.ts` (44 B),
  `src/ui/timers.ts` (43 B), `src/ui/styles/composer.css` (65 B) --
  all byte-identical to last review.
- **F16 unchanged.** Same 12 orphan comment-only shims under
  `src/tool/*.ts` (`apply-patch.ts`, `application-tools.ts`, `bash.ts`,
  `builtins.ts`, `edit.ts`, `glob.ts`, `native.ts`, `notebook.ts`,
  `question.ts`, `read-filesystem.ts`, `todowrite.ts` + the
  `warpgrep.ts` tombstone). Eighth review.
- **F21 dispositions:** four zero-inbound top-level modules unchanged
  (`undo/`, `update/`, `share/`, `sync/`). Two-cycle grace expired
  as promised last cycle; per that disposition **each drops to `info`
  this cycle**. Recommendation 4 keeps them in the subtractive PR set
  but stops counting them as review-time noise.
- **F2 unchanged.** `src/cli/commands/models.ts:6` still imports
  `DeploymentApi` from `@sap-ai-sdk/ai-api`. **Eighth review.** Migration
  path is unchanged and now trivially mechanical (drop the direct
  import, use `getDeploymentApiClient` from
  `src/providers/modelCatalog.ts`).
- **Recommendation 3 (sync-allowlist guardrail) STILL not landed.**
  **EIGHTH consecutive review requesting it.** F29 this cycle is one
  hit it would have caught (new file inside existing dir, zero
  incoming refs from outside that dir, not test). This is now the
  single longest-running unaddressed architectural recommendation in
  the series.

Net effect: **worse than last cycle** despite lower merge volume.
Auto-delete deadlines were set six weeks ago and re-affirmed last
cycle; both slipped. F20 hits its **fourth** consecutive cycle at
`block`. F24's escalation clause fired. F26's grace-period recommendation
was ignored. Zero new dead sub-directories is the only genuine win.

## Findings

### F1..F14, F16..F28 -- carried forward, verified

Verification pass for prior findings from `REVIEW-2026-08-31` (status
shown; see prior review for context and citations).

- **F1** `block`: no ADRs 002..N. `docs/adr/` still has exactly one
  substantive ADR (`001-establish-adr-process.md`) plus ten
  `REVIEW-*.md` snapshots (including this one).

- **F2** `block`: `src/cli/commands/models.ts:6` still imports
  `DeploymentApi` from `@sap-ai-sdk/ai-api`. Confirmed via
  `grep -rln "@sap-ai-sdk\|@ai-sdk" src/ --include='*.ts'`:
  matches remain `src/cli/commands/models.ts`,
  `src/providers/openai/prompt-cache.ts`,
  `src/providers/sapOrchestration.ts`, `src/providers/transform.ts`,
  `src/providers/modelCatalog.ts`. `models.ts` remains the sole SDK
  import outside `src/providers/`. **Eighth review.** Recommendation 9
  restated.

- **F3** `warn`: unchanged. `models.ts` still bypasses the interface.

- **F4** `warn`: `agentsMdReminders` callers from `src/tool/tools/`
  unchanged. See F17.

- **F5** -> CLOSED (still closed).

- **F6** `block`: `src/core/migration/` unchanged. Same seven files.
  Ninth review. No loader, no `better-sqlite3` dep, no ADR-004.

- **F7** `info`: `.github/prompts/role-architecture.md` module map
  still stale in one place -- reads "grown to **35 top-level
  directories**" in the vertical-knowledge section. `docs/ARCHITECTURE.md`
  tree section lists 35 entries and its prose (line 2098) also says
  "35 modules". Actual on-disk count is **35**. Both are correct
  this cycle. F7 stays `info` and will be closed next cycle if the
  count holds.

- **F8** `info`: ESM `.js` import discipline confirmed clean. Full
  scan `grep -rEn "from ['\"]\.\.?/[^'\"]+['\"]" src/ --include='*.ts'
  --include='*.tsx' | grep -vE "\.js['\"]$|\.(json|css|txt)['\"]$"`
  returns zero real hits (all matches are inside
  `src/context/__tests__/ranking.test.ts` template-literal fake
  source strings for the import-extractor tests, plus a couple of
  re-export list entries that already end in `.js`). No real
  imports missing `.js` exist under `src/`.

- **F9** `info`: `src/plugin/__tests__/ruleCommand.test.ts:47` still
  imports `../../agent/system.js`. Test-only cross-layer import.
  Unchanged.

- **F10** `block`: `src/core/billing.ts` still 188 bytes. Still
  exports `paymentOptions` (Stripe `off_session` on `card`+`link`).
  Still zero references. **Sixth review.** Same disposition:
  Recommendation 4.

- **F11** `block`: `src/core/catalog.ts` grew slightly to 4256 B
  (was 3778 B last cycle) but still zero consumers repo-wide
  (`grep -rn "from ['\"][^'\"]*core/catalog" src/ --include='*.ts'`).
  **Sixth review.** Still demonstrably superseded by
  `src/providers/modelCatalog.ts`. The size growth is a modest
  automated churn (comment/JSDoc drift) with no wiring change,
  which strengthens rather than weakens the "delete this" case.

- **F12** -> CLOSED (still closed).

- **F13** `block`: partial, unchanged. `src/session/data.ts` (44 B),
  `src/ui/timers.ts` (43 B), `src/ui/styles/composer.css` (65 B)
  all still present, byte-identical to last review.

- **F14 (was: sync-allowlist guardrail not filed)** `info`:
  superseded by Recommendation 3 (see "Recommendations" below).
  **Eighth cycle without action.**

- **F15** -> CLOSED (unchanged).

- **F16** `block`: same 12 orphan comment-only shims. No cycle
  change. Consumer scan for each returns zero external hits.
  Eighth review.

- **F17** `block`: ADR-001 rule-4 violations in `src/tool/`
  unchanged this cycle. `grep` confirmation:

  - `src/tool/tools/edit.ts:9` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/read.ts:19` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/multiedit.ts:9` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/write.ts:11` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/grep.ts:19` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/task.ts:30` -> `../../agent/index.js`
    (`getAgentRegistry`, `Agent`)
  - `src/tool/tools/task.ts:31` -> `../../core/costTracker.js`
  - `src/tool/tools/memory.ts:8-9` -> `../../core/memory.js`

  Fix options unchanged from previous reviews. Seventh review.

- **F18** `block`: `src/permission/index.ts:26` still imports
  `getAllToolNames` from `../tool/registry.js`. Confirmed via
  `grep -rEn "from ['\"][^'\"]+['\"]" src/permission/ | grep -E "['\"](\.\.?/)+tool/"`.
  Test file `src/permission/__tests__/wildcard-tools.test.ts:9,10`
  also imports from `../../tool/index.js` and `../../tool/tools/index.js`
  (test-only, carved out). **Eighth review.** Same fix options.

- **F19** `info`: `src/tool/tools/` heterogeneity unchanged;
  `docs/TOOLS.md` still hand-maintained.

- **F20** `block`: `src/core/router.ts:12` still imports
  `c` from `../cli/utils/colors.js`. **Fourth cycle at `block`.**
  Longest-running rule-4 violation in the series. See Recommendation 5.

- **F21** `warn` -> `info` **(demoted per last cycle's disposition)**:
  four zero-inbound top-level modules unchanged from last cycle
  (`undo/`, `update/`, `share/`, `sync/`). Two-cycle grace period
  expired. Per `REVIEW-2026-08-31` Recommendation 7, each drops to
  `info` today. They remain in the subtractive PR list
  (Recommendation 4) but stop being counted as review-time findings.

- **F22** `warn` -> **`block` (auto-delete deadline slipped)**:
  `src/session/reminders.ts` (3041 B) and
  `src/session/ask-code-switch.txt` (135 B) both still zero-inbound
  (`grep -rEn "from ['\"][^'\"]*session/reminders" src/` returns
  zero, `grep -rn "ask-code-switch" src/` returns zero). Last cycle
  set today as the auto-delete deadline. Slipped. Fourth review.
  Escalated per commitment; see Recommendation 4.

- **F23** `warn` -> **`block` (auto-delete deadline slipped)**:
  `src/tool/grep.ts` (754 B) + `src/tool/grep-signal-controls.ts`
  (3462 B) re-export chain still zero-inbound
  (`grep -rEn "from ['\"][^'\"]*tool/grep(-signal-controls)?['\"]"
  src/ --include='*.ts'` returns zero). Same auto-delete deadline,
  same slip. Fourth review. Escalated per commitment.

- **F24** `warn` -> **`block` (escalation clause fired)**:
  `src/kilocode/` (top-level) unchanged. Still one file
  (`sandbox/git.ts`), still wired by `src/tool/tools/shell.ts:18`,
  **still no ADR 006** (`ls docs/adr/006* -> no such file`). Last
  cycle set today as the "ADR OR rename" deadline. Neither
  happened. Escalated to `block` as promised. See Recommendation 2.

- **F25** `warn`: `src/core/session/*` unchanged. All four files
  (`store.ts`, `overflow.ts`, `processor.ts`, `retry.ts`) still
  present, still zero external references
  (`grep -rEn "from ['\"][^'\"]*/session/(store|overflow|processor|retry)"
  src/` returns zero external hits). Held at `warn` this cycle.
  Two-cycle grace period expires 2026-09-14; escalates to `block`
  then if still unwired.

- **F26** `warn` -> **`block`**: `src/core/pty/termination.ts`
  unchanged, still zero external references
  (`grep -rEn "from ['\"][^'\"]*core/pty" src/` returns zero).
  Last cycle's recommendation ("wire into bash-tool kill path OR
  delete this cycle") was not acted on. Escalated to `block` per
  the recommendation's contract. 2026-09-14 deadline for
  disposition.

- **F27** `warn`: `src/core/kilocode/zero-id.ts` still zero-inbound
  (`grep -rEn "kilocode/zero-id" src/` returns only its own
  declaration). `src/core/kilocode/fff.ts` still wired only from
  `src/core/filesystem/watcher.ts:35` (unchanged). Held at `warn`
  because F27 is subordinate to ADR 006 (F24) -- once F24
  resolves, F27 disposition follows automatically.

- **F28** `info`: two `agent-manager.ts` files unchanged. Still a
  navigation wart, not a violation. Same disposition (rename
  tool-side file when it next gets substantive work).

### F29 `warn` -- NEW: `src/providers/gitlab.ts` landed zero-inbound

`src/providers/gitlab.ts` (100 lines) landed via
`e87512ed feat(sync): apply upstream changes (2026-09-06) (#1657)`.
The file exposes a GitLab provider surface -- OAuth token exchange,
identity claim shaping -- alongside a test at
`src/providers/__tests__/gitlab.test.ts:1` (67 lines).

Consumer scan:

- `grep -rEn "from ['\"][^'\"]*providers/gitlab" src/` returns zero
  external hits.
- `grep -rEn "providers/gitlab" src/` returns only the test file
  importing its own module.

Layering compliance: `src/providers/gitlab.ts` sits inside
`src/providers/`, so SDK usage is legal per role-architecture.md's
layer diagram. This is NOT a rule-4 violation.

Structural concern: same F21 / F25 / F26 pattern one layer over --
upstream sync channel pre-lands a working module with no local
caller. The obvious call site would be `src/providers/index.ts`'s
`getProviderForModel` dispatch, but `index.ts` does not reference
`gitlab.ts`. Neither does `src/providers/auth.ts` (the OAuth broker
used by other providers).

Severity `warn`. Disposition: wire into `src/providers/index.ts`
this cycle OR delete on 2026-09-14 alongside the F25 subtractive
PR. Owner: `role-engineering` (wire) or `role-architecture`
(subtract).

### F30 `info` -- NEW-ish: `src/providers/protocols/` sub-directory zero-inbound

`src/providers/protocols/` contains `shared.ts` + `shared.test.ts`.
`grep -rEn "from ['\"][^'\"]*providers/protocols" src/` returns
zero external hits.

Verifying git history for landing date is left to next cycle if
this persists; recording as `info` for now because (a) it is a
lower-severity subdirectory than the F25/F26 core adds, (b) the
`shared.ts` name suggests future-shared code and it may wire this
cycle. If still zero-inbound at `REVIEW-2026-09-14`, promote to
`warn` and add to the subtractive PR.

## Recommendations

Priority order for the next fortnight (mostly carried forward;
one new item for F29):

1. **Wire OR delete the F26 pty and F29 gitlab modules THIS
   CYCLE.** F26 has now missed its "this cycle" deadline from last
   review; F29 is new. Both are the same shape: a working module
   inside a legal layer with no local caller. `role-engineering`
   owns the wiring decision (bash-tool kill path for pty,
   `providers/index.ts` dispatch for gitlab); `role-architecture`
   owns the subtract-if-unwired PR on 2026-09-14.

2. **File ADR 006 for `src/kilocode/` and `src/core/kilocode/`.**
   Escalated to `block` (F24 escalation clause fired). ADR 006
   must resolve BOTH directories (see F27 above; three concrete
   dispositions from last cycle unchanged). Rename `fff.ts` to a
   descriptive name in the same PR. Owner: this vertical. Scope:
   `docs(core)`.

3. **Sync-allowlist guardrail (`role-infrastructure`).** **EIGHTH
   consecutive review requesting this.** F29 is one hit it would
   have caught. Contract unchanged from
   `REVIEW-2026-08-31` Recommendation 3 -- fail sync PRs that
   introduce:

   - A new top-level `src/<name>/` directory not accompanied by
     `docs/adr/NNN-*.md` in the same diff.
   - A NEW **sub-directory** under an existing `src/<name>/`
     containing only files with zero incoming references from
     outside that directory, and not test files.
   - A **new file** inside an existing `src/<name>/` directory
     with zero incoming references from outside that directory,
     not a test, larger than 20 lines of executable code.
     **NEW clause this cycle -- motivated by F29** (100-line
     working provider module, no dispatcher registration).
   - Files matching `payment*`, `billing*`, `*credentials*`,
     `*stripe*`, `*catalog*`.
   - Empty-on-purpose files: size < 200 bytes AND consisting only
     of blank lines and single-line `//` comments (outside
     `docs/`, `.github/`, `.specify/`).
   - Any file with extension `.kt`, `.kts`, `.java`, `.swift`,
     `.rb`.
   - New file with an opaque three-or-fewer-letter basename
     (`fff.ts`, `zzz.ts`).

4. **Combined subtractive cleanup PR -- `chore(sync)`.** Updated
   this cycle to execute F22 / F23 auto-delete (deadline expired
   today) and stage F26 / F29 for the 2026-09-14 deadline:

   ```sh
   # F10 / F11 / F13 (all sixth review, zero refs)
   git rm src/core/billing.ts
   git rm src/core/catalog.ts
   git rm src/session/data.ts
   git rm -r src/ui

   # F16 -- 12 comment-only shims (eighth review)
   git rm src/tool/apply-patch.ts src/tool/application-tools.ts \
          src/tool/bash.ts src/tool/builtins.ts src/tool/edit.ts \
          src/tool/glob.ts src/tool/native.ts \
          src/tool/notebook.ts src/tool/question.ts \
          src/tool/read-filesystem.ts \
          src/tool/todowrite.ts

   # F22 -- auto-delete deadline slipped, executing per commitment
   git rm src/session/reminders.ts src/session/ask-code-switch.txt

   # F23 -- auto-delete deadline slipped, executing per commitment
   git rm src/tool/grep.ts src/tool/grep-signal-controls.ts

   # F26 -- if still unwired at 2026-09-14
   git rm -r src/core/pty

   # F29 -- if still unwired at 2026-09-14
   git rm src/providers/gitlab.ts src/providers/__tests__/gitlab.test.ts

   # F27 -- pending ADR 006 verdict
   # git rm src/core/kilocode/zero-id.ts
   ```

   Verify with `npm run typecheck && npm run lint && npm run build`
   and `npm run test:coverage`. F27 held back until ADR 006 lands.

5. **Fix F20 -- fourth cycle at `block`.** Two options unchanged:
   (a) make `src/core/router.ts` return structured decisions and
   move colouring to `src/cli/` -- preferred; (b) move
   `src/cli/utils/colors.ts` to `src/utils/colors.ts` and rewrite
   the router import. This has now aged more than any other
   rule-4 violation in the series. Owner: `role-engineering`.

6. **Close F17 / F18 (unchanged).** One PR scoped
   `refactor(tools)` or two paired PRs (`refactor(tools)` +
   `refactor(permission)`). Eighth review of each.

7. **F21 -> `info` this cycle.** Handled by demoting; no further
   review-time cost until the subtractive PR at Recommendation 4
   lands them.

8. **F22 / F23 -- execute the delete.** Auto-delete deadline slipped
   today. Do NOT defer again; a fifth review at `block` with the
   same disposition is not productive. Land as part of
   Recommendation 4 within the fortnight.

9. **F2 / F3 (unchanged).** Migrate `src/cli/commands/models.ts:6`
   off `@sap-ai-sdk/ai-api`. Mechanically trivial since
   `src/providers/modelCatalog.ts` provides a ready-made shared
   client. Ninth review of this ask.

10. **F6 -- migration directory decision (unchanged).** Ninth
    review. Either ADR-004 with SQLite + loader + tests, or
    `git rm -r src/core/migration/`.

11. **ADR 002 baseline -- deferred (unchanged).** File AFTER
    Recommendation 4 lands AND F22/F23 physically delete AND
    F24/F27 resolve via ADR 006.

## ADR needed?

**Yes -- ADR 006 for `src/kilocode/` AND `src/core/kilocode/`
(see Recommendation 2).** Escalated to `block` this cycle because
F24's ADR-or-rename deadline expired today with neither action.

Potential future ADRs (unchanged from last cycle):

- ADR 002 -- baseline (after cleanup).
- ADR 003 -- `permission/ -> tool/registry.ts` carve-out (only if
  F18 fix chooses that path over inversion).
- ADR 004 -- migration directory verdict (F6).
- ADR 005 -- `cli/commands/models.ts` `@sap-ai-sdk/ai-api`
  exception or refactor (F2).
- **ADR 006 -- `src/kilocode/` and `src/core/kilocode/` charter
  or rename (F24, F27). ESCALATED TO `block` THIS CYCLE.**

Sync-channel rule tripping tally this cycle:

- Rule 3 at **top-level** (new `src/<name>/`): **0 hits.**
- Rule 3 at **sub-directory level** (new sub-dir under existing
  `src/<name>/` with zero external refs, not tests): **0 hits.**
  Direct improvement over last cycle's three hits.
- Rule 3 at **file level** (new file inside existing dir, zero
  incoming refs from outside that dir, not test, >20 LoC):
  **1 hit** -- `src/providers/gitlab.ts` (F29). Motivates the new
  file-level clause of Recommendation 3.
- Rule 4 (upward import): F20 survives at `block` for a fourth
  consecutive cycle. F17 / F18 unchanged. Zero NEW rule-4 hits.

Direction of travel is mixed. Zero new top-level directories,
zero new sub-directory adds, and F8 (ESM discipline) confirmed
still clean is genuine architectural progress. But the ledger of
carried-forward findings continues to grow rather than shrink:
F10/F11/F13/F16/F17/F18/F20 are all at six-to-nine consecutive
`block` reviews, and F22/F23 slipped an explicit auto-delete
commitment. Two escalation clauses (F24 -> `block`,
F26 -> `block`) fired as promised, so the review process is at
least self-consistent. But if `REVIEW-2026-09-14` shows F22/F23
still present, F26 still unwired, and F24 still without ADR 006,
the pattern shifts from "deferred cleanup" to "the review is
being ignored" and Recommendation 4 needs to be executed by this
vertical directly rather than requested from
`role-engineering`.
