# Architecture Review -- 2026-09-14 (scheduled, master)

- Trigger: `schedule` on `refs/heads/master`
- HEAD: `c389d63d` (`docs(ci): planning brief 2026-09-14 [alexi-bot]`)
- Previous reviews: `001-establish-adr-process.md`, `REVIEW-2026-06-04.md`,
  `REVIEW-2026-06-04T12-followup.md`, `REVIEW-2026-06-15.md`,
  `REVIEW-2026-06-29.md`, `REVIEW-2026-07-06.md`, `REVIEW-2026-07-27.md`,
  `REVIEW-2026-08-17.md`, `REVIEW-2026-08-24.md`, `REVIEW-2026-08-31.md`,
  `REVIEW-2026-09-07.md`

## Summary

Eleventh scheduled architecture run. Merge traffic in the window since
`REVIEW-2026-09-07` includes three `feat(sync)` PRs (2026-09-08 #1681,
2026-09-11 #1699, 2026-09-12 #1710, 2026-09-13 #1719), two direct
`feat(providers|core)` PRs (#1712 AI SDK OTLP relay, #1718 inline model
override), one shared-board env-flag path (#1706), a home-directory index
guard (#1688), a mistake-limit steering prompt (#1696), a URL/file:line
auto-linkifier (#1695), a TUI Home/End cursor fix (#1677), and the usual
dependabot + auto-docs churn.

**Headline architectural news in this run:**

- **NO new top-level `src/<name>/` directory landed.** Third cycle running
  clean at directory level. Top-level count stable at **35**
  (`ls -1 src/ | wc -l` -> `35`).
- **ONE new sub-directory landed under an existing layer:**
  `src/providers/protocols/` (`shared.ts` + `shared.test.ts`, 69 lines).
  F30 promotes from `info` -> `warn` this cycle per its own
  disposition: zero external inbound references
  (`grep -rEln "from ['\"][^'\"]*/protocols/shared" src/` returns only
  the co-located test file).
- **FIVE new zero-inbound files landed across four layers** via the
  2026-09-11 and 2026-09-13 sync PRs (F31 -> F35). None violate
  rule-4 (all sit inside a legal layer). All are the same F21 / F25 /
  F26 / F29 shape -- upstream sync pre-lands working modules with no
  local caller. See Findings F31..F35.
- **NEW USE-SITES restored some prior warnings to normal:** the
  weekly window added actual wiring for several files that were
  zero-inbound at landing time. Specifically:
  - `src/cli/tui/utils/linkify.ts` -> wired from
    `src/cli/tui/components/ToolRow.tsx:15`.
  - `src/core/loopDetector.ts` + `src/core/mistakeTracker.ts` ->
    wired from `src/core/agenticChat.ts:41,42`.
  - `src/cli/utils/mistakeLimitPrompt.ts` -> wired from
    `src/cli/commands/agent.ts:26`.
  - `src/core/inlineModelOverride.ts` -> wired from
    `src/core/orchestrator.ts:15` and
    `src/core/streamingOrchestrator.ts:30`.
  - `src/tool/tools/open-plan.ts` -> wired from
    `src/tool/tools/index.ts:45,109,173`.
  - `src/providers/model-selection.ts` -> wired from
    `src/providers/index.ts:78` and three tool consumers.
  - `src/utils/tracing.ts` -> wired from `src/cli/program.ts:15`
    and `src/providers/sapOrchestration.ts:66`.

  These are recorded as **info** below (F36..F42) with no action --
  they demonstrate that the sync-channel does self-correct for the
  simpler cases within a cycle.
- **Rule 4 (F20) STILL not fixed.** `src/core/router.ts:12` still
  imports `c` from `../cli/utils/colors.js`. **Fifth consecutive
  cycle at `block`.** Now formally the longest-running unresolved
  rule-4 violation in the series.
- **F17 unchanged.** All eight documented `src/tool/tools/*.ts` ->
  `src/agent/` and `src/tool/tools/*.ts` -> `src/core/` upward imports
  are still there byte-identical. Eighth review.
- **F18 unchanged.** `src/permission/index.ts:26` still imports
  `getAllToolNames` from `../tool/registry.js`. Ninth review.
- **F22 / F23 -- deletes STILL not executed.** `REVIEW-2026-09-07`
  escalated both to `block` after the auto-delete deadline slipped.
  Fifth review at `block` with same disposition. Files unchanged
  byte-identical.
- **F24 (kilocode/) -- ADR 006 STILL missing.** `ls docs/adr/006*`
  returns no file. Second cycle at `block` (escalated last cycle
  when the ADR-or-rename deadline expired). `src/kilocode/` now has
  a NEW sub-directory `src/kilocode/board/` containing
  `enabled.ts` (43 LoC, zero external inbound) -- see F31. So the
  `kilocode/` situation has *grown* while awaiting its charter ADR.
- **F25 -- escalation clause fires this cycle as promised.**
  `src/core/session/*` unchanged (`store.ts`, `overflow.ts`,
  `processor.ts`, `retry.ts` all still zero external inbound). Last
  cycle's two-cycle grace period explicitly expired today. Promoted
  to `block`.
- **F26 -- pty termination STILL unwired.** Unchanged from last
  cycle. Second review at `block`. 2026-09-14 deadline for
  disposition (this cycle) triggers subtract-in-recommendation-4.
- **F27 -- subordinate to ADR 006, held at `warn`** (F24 unresolved).
  `src/core/kilocode/zero-id.ts` still zero-inbound.
  `src/core/kilocode/fff.ts` still wired only from
  `src/utils/filesystem.ts:8` (path unchanged; previously listed as
  `src/core/filesystem/watcher.ts:35` in the last two reviews --
  see F27 correction note). Newly added `src/core/kilocode/pty/`
  sub-directory (F32) increases the kilocode carve-out surface.
- **F29 -- gitlab provider STILL zero-inbound one week later.**
  `src/providers/gitlab.ts` (3749 bytes, up from 3702 last cycle --
  automated churn only) and `src/providers/__tests__/gitlab.test.ts`
  both still present. `src/providers/index.ts::getProviderForModel`
  does not reference it. Second review at `warn`. Per last cycle's
  disposition ("wire OR delete on 2026-09-14"), this cycle escalates
  to `block` (Recommendation 1).
- **F30 -- providers/protocols sub-directory STILL zero-inbound.**
  Second observation, promoted `info` -> `warn` per last cycle's
  disposition. Two-cycle grace ends 2026-09-21.
- **Recommendation 3 (sync-allowlist guardrail) STILL not landed.**
  **NINTH consecutive review requesting it.** F31..F35 in this
  cycle alone are five hits it would have caught. This vertical
  now escalates: unless `role-infrastructure` picks it up, this
  review will file it directly by 2026-09-21 (see Recommendation
  3 below).

Net effect: **materially worse than last cycle.** F25 escalated to
`block` as promised. F29 escalated to `block` as promised. F30
promoted to `warn` as promised. Five new zero-inbound files landed
in one week (F31..F35). The ledger of carried-forward `block`
findings is now:
F1, F2, F6, F10, F11, F13, F16, F17, F18, F20, F22, F23, F24, F25,
F26, F29 = **sixteen `block`-severity findings unresolved**, up
from twelve last cycle. Cleanup PR from Recommendation 4 continues
to be the highest-leverage action.

## Findings

### F1..F14, F16..F30 -- carried forward, verified

Verification pass for prior findings from `REVIEW-2026-09-07` (status
shown; see prior review for context and citations).

- **F1** `block`: no ADRs 002..N. `docs/adr/` still contains exactly
  one substantive ADR (`001-establish-adr-process.md`) plus eleven
  `REVIEW-*.md` snapshots (including this one). Unchanged.

- **F2** `block`: `src/cli/commands/models.ts:6` still imports
  `DeploymentApi` from `@sap-ai-sdk/ai-api`
  (`grep -rEn "from ['\"]@sap-ai-sdk|from ['\"]openai['\"]|from
  ['\"]@anthropic" src/ | grep -v "src/providers/"` returns exactly
  this one hit). **Ninth review.** Recommendation 9.

- **F3** `warn`: unchanged. Cascading from F2.

- **F4** `warn`: `agentsMdReminders` callers unchanged. See F17.

- **F5** -> CLOSED (still closed).

- **F6** `block`: `src/core/migration/` unchanged. Same seven files
  (`legacy_migration.ts`, `event_sourced_session_input.sql`,
  `20260127*`, `20260211*`, `20260511173437_session-metadata.sql`,
  `session_input_inbox_snapshot.json`,
  `session_message_projection_order.sql`). Tenth review. No loader,
  no `better-sqlite3` dep, no ADR-004.

- **F7** `info`: `role-architecture.md:15` vertical-knowledge prose
  says "grown to **35 top-level directories**". Actual on-disk
  count is 35 (`ls -1 src/ | wc -l` -> `35`). `docs/ARCHITECTURE.md`
  line 2317 also says "35 modules". All three are consistent this
  cycle. Held at `info`; closes on next cycle if the count holds.

- **F8** `info`: ESM `.js` import discipline confirmed clean. Full
  scan `grep -rEn "from ['\"]\.\.?/[^'\"]+['\"]" src/ | grep -vE
  "\.js['\"]|\.(json|css|txt|md)['\"]"` returns the same
  template-literal test fixtures under
  `src/context/__tests__/ranking.test.ts` (fake source strings for
  the import extractor -- not real ESM imports). Zero real
  offenders. Unchanged.

- **F9** `info`: `src/plugin/__tests__/ruleCommand.test.ts:47` still
  imports from `../../agent/system.js`. Test-only cross-layer
  import. Unchanged.

- **F10** `block`: `src/core/billing.ts` still 188 bytes. Still
  zero references. **Seventh review.** Recommendation 4.

- **F11** `block`: `src/core/catalog.ts` still 4256 bytes. Still
  zero consumers repo-wide. **Seventh review.** Recommendation 4.

- **F12** -> CLOSED.

- **F13** `block`: partial, unchanged. `src/session/data.ts`
  (44 B), `src/ui/timers.ts` (43 B), `src/ui/styles/composer.css`
  (65 B) all still present, byte-identical. `grep -rEn "from
  ['\"][^'\"]*(session/data|ui/timers|ui/styles/composer)" src/`
  returns zero. Recommendation 4.

- **F14** `info`: superseded by Recommendation 3. **Ninth cycle
  without action.**

- **F15** -> CLOSED.

- **F16** `block`: same 12 orphan comment-only shims under
  `src/tool/*.ts`. Sizes verified via `ls -la` -- byte-identical
  to last review (`apply-patch.ts` 35 B, `application-tools.ts`
  96 B, `bash.ts` 27 B, `builtins.ts` 90 B, `edit.ts` 27 B,
  `glob.ts` 27 B, `native.ts` 35 B, `notebook.ts` 79 B,
  `question.ts` 31 B, `read-filesystem.ts` 33 B, `todowrite.ts`
  32 B, `warpgrep.ts` 654 B tombstone). Ninth review.

- **F17** `block`: ADR-001 rule-4 violations in `src/tool/`
  unchanged this cycle. Confirmation via
  `grep from ['\"](\.\.\/)+(cli|agent)\/ src/tool` returns:

  - `src/tool/tools/edit.ts:9` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/read.ts:19` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/multiedit.ts:9` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/write.ts:11` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/grep.ts:19` -> `../../agent/agentsMdReminders.js`
  - `src/tool/tools/task.ts:30` -> `../../agent/index.js`
    (`getAgentRegistry`, `Agent`)
  - `src/tool/tools/task.ts:31` -> `../../core/costTracker.js`
    (note: no direct hit for `costTracker` from `grep` this cycle
    because `task.ts:31` was verified inline; unchanged from
    last cycle listing)
  - `src/tool/tools/memory.ts:8-9` -> `../../core/memory.js`

  Fix options unchanged. Eighth review.

- **F18** `block`: `src/permission/index.ts:26` still imports
  `getAllToolNames` from `../tool/registry.js`. Confirmed. Ninth
  review.

- **F19** `info`: `src/tool/tools/` heterogeneity unchanged.

- **F20** `block`: `src/core/router.ts:12` still imports
  `c` from `../cli/utils/colors.js`. **Fifth cycle at `block`.**
  Now formally the longest-running rule-4 violation. Recommendation 5.

- **F21** `info`: four zero-inbound top-level modules unchanged
  (`undo/`, `update/`, `share/`, `sync/`). Held at `info` per last
  cycle. Included in Recommendation 4 subtractive PR.

- **F22** `block`: `src/session/reminders.ts` (3041 B) and
  `src/session/ask-code-switch.txt` (135 B) both still zero-inbound
  and byte-identical to last review. Auto-delete deadline slipped
  a second cycle. **Fifth review at `block`.** Recommendation 4
  (execute this cycle -- see also Recommendation 8).

- **F23** `block`: `src/tool/grep.ts` (754 B) +
  `src/tool/grep-signal-controls.ts` (3462 B) still zero-inbound.
  Byte-identical to last review. **Fifth review at `block`.**
  Recommendation 4.

- **F24** `block`: `src/kilocode/` unchanged charter-wise. Still no
  ADR 006 (`ls docs/adr/006*` returns nothing). **Grew** this cycle
  by one file (F31, `src/kilocode/board/enabled.ts`). Consumer
  situation: `src/kilocode/sandbox/git.ts` still wired by
  `src/tool/tools/shell.ts:18`; `src/kilocode/board/enabled.ts`
  zero-inbound. Second cycle at `block`. Recommendation 2 unchanged;
  ADR 006 must now cover THREE contents (sandbox/, board/, and the
  `src/core/kilocode/` twin).

- **F25** `warn` -> **`block` (escalation clause fired)**:
  `src/core/session/*` unchanged. All four files still present, all
  four still zero external inbound (`grep -rEn "from
  ['\"][^'\"]*core/session/(store|overflow|processor|retry)" src/`
  returns zero). Sizes: `store.ts` (84 B), `overflow.ts`
  (1730 B), `processor.ts` (2454 B), `retry.ts` (4905 B). Last
  cycle's two-cycle grace expired today as promised. Escalated
  per contract. Recommendation 4 lists a subtract clause.

- **F26** `block`: `src/core/pty/termination.ts` unchanged, still
  zero external references. Second cycle at `block`. 2026-09-14
  deadline for disposition (this cycle). Recommendation 4
  subtracts the file.

- **F27** `warn`: `src/core/kilocode/zero-id.ts` unchanged
  (1464 B, zero external inbound). `src/core/kilocode/fff.ts`
  (4307 B) still wired from `src/utils/filesystem.ts:8` -- this
  corrects the last two reviews which listed the caller as
  `src/core/filesystem/watcher.ts:35`. The `fff.ts` -> `allowed`
  import (via `require`/`import { allowed } from
  '../core/kilocode/fff.js'`) has been in `src/utils/filesystem.ts`
  since at least the F27 opening cycle; the earlier review
  citations were stale. Held at `warn` (subordinate to ADR 006 /
  F24). The new sibling `src/core/kilocode/pty/latch.ts` (F32)
  extends the kilocode carve-out further.

- **F28** `info`: two `agent-manager.ts` files unchanged.
  `src/permission/agent-manager.ts` is wired from
  `src/tool/tools/agent-manager.ts:7` AND `src/core/orchestration.ts:25`
  (`grep from ['\"][^'\"]*agent-manager\.ts|agent-manager\.js` src/`
  returns three hits including the tool `index.ts:40` self-import).
  Still a navigation wart, not a violation.

- **F29** `warn` -> **`block` (deadline slipped as promised)**:
  `src/providers/gitlab.ts` (3749 B, +47 B vs last cycle from
  automated doc/comment churn only) still zero external inbound.
  `src/providers/__tests__/gitlab.test.ts` present. Neither
  `src/providers/index.ts::getProviderForModel` nor
  `src/providers/auth.ts` references it. **Second review**, deadline
  from `REVIEW-2026-09-07` disposition expired today.
  Escalated to `block`. Recommendation 4 includes the subtract clause.

- **F30** `info` -> **`warn`**: `src/providers/protocols/`
  sub-directory still contains only `shared.ts` (69 lines) +
  `shared.test.ts`. `grep -rEln "from ['\"][^'\"]*/protocols/shared"
  src/` returns only the co-located test file. Promoted per last
  cycle's disposition (F30 opened at `info` with
  "if still zero-inbound at REVIEW-2026-09-14, promote to `warn`").
  Two-cycle grace begins today; escalates to `block` on 2026-09-21
  if unresolved.

### F31 `warn` -- NEW: `src/kilocode/board/enabled.ts` landed zero-inbound

`src/kilocode/board/enabled.ts` (43 LoC) landed via
`6276dfbd feat(sync): apply upstream changes (2026-09-11)`. It exports
a feature-flag reader for the "kilocode board" enablement path
introduced by PR #1706 (`feat(config): add env flag enable path for
shared agent board`, 4c20df6b).

Consumer scan:

- `grep -rEln "from ['\"][^'\"]*kilocode/board" src/` returns zero.
- `grep -rEn "kilocode\.board|kilocodeBoard|kilocodeAgentBoard|
  boardEnabled" src/` returns zero hits under `src/`, though the
  config-layer feature (#1706) does exist elsewhere. The reader
  exported from `enabled.ts` is not called anywhere.

Layering compliance: the file sits inside `src/kilocode/`, which is
already subject to F24. So this is NOT a rule-4 violation but is
the same F21 / F25 / F26 / F29 shape one directory over.

**Additional escalation:** this file materially extends the surface
area of `src/kilocode/` while ADR 006 (F24) is still missing.
Recommendation 2 (file ADR 006) now needs to cover THREE contents:
`sandbox/`, `board/`, and the twin `src/core/kilocode/`.

Severity `warn`. Disposition: wire from the config layer (obvious
call site: `src/config/userConfig.ts` or wherever
`AGENT_BOARD_ENABLED`-style flags are read) OR delete on
2026-09-21. Owner: `role-engineering` (wire) or `role-architecture`
(subtract).

### F32 `warn` -- NEW: `src/core/kilocode/pty/latch.ts` landed zero-inbound

`src/core/kilocode/pty/latch.ts` (74 LoC) landed via
`6276dfbd feat(sync): apply upstream changes (2026-09-11)`. Creates a
new sub-directory `src/core/kilocode/pty/` under the already-flagged
`src/core/kilocode/` (F27).

Consumer scan:

- `grep -rEln "from ['\"][^'\"]*(latch|kilocode/pty)" src/` returns
  zero external hits.
- Search for `Latch` symbol across `src/` finds only its own
  declaration.

Layering compliance: sits inside `src/core/kilocode/`, already
subject to F24 / F27. Not a rule-4 violation but expands the
kilocode surface further while ADR 006 is missing.

Severity `warn`. Overlaps with F26 (existing `src/core/pty/` also
zero-inbound). Recommendation 4 includes a subtract clause with
2026-09-21 deadline.

### F33 `warn` -- NEW: `src/providers/bedrock-model-id.ts` landed zero-inbound

`src/providers/bedrock-model-id.ts` (78 LoC) landed via
`6276dfbd feat(sync): apply upstream changes (2026-09-11)`. Exposes
a Bedrock model-id parser/normaliser.

Consumer scan:

- `grep -rEln "from ['\"][^'\"]*/bedrock-model-id" src/` returns zero.
- `grep -rEn "bedrock-model-id|bedrockModelId" src/` returns zero
  hits outside the file itself.

Note: extensive Bedrock handling already exists inside
`src/providers/transform.ts` (`filterUnreplayableBedrockReasoning`,
`hasBedrockReasoningSignature`, etc.). The new file is a NEW
utility with no consumer -- either it's meant to replace some of
the inline logic in `transform.ts` (in which case that migration
was not landed with the file) or it is unused.

Layering compliance: sits inside `src/providers/`, so SDK usage is
legal. Not a rule-4 violation. Same F29 shape (provider-layer
zero-inbound module).

Severity `warn`. Disposition: wire into `transform.ts` (or wherever
Bedrock model IDs are currently parsed inline) OR delete on
2026-09-21. Owner: `role-engineering`.

### F34 `info` -- NEW: `src/providers/protocols/shared.ts` (already covered under F30)

Recorded here for completeness of the 2026-09-11..2026-09-13 sync
window. Full disposition tracked under F30 (see above).

### F35 `warn` -- NEW: `src/core/database/migrations/2026090*.ts` landed zero-inbound

Two new migration files landed via
`6276dfbd feat(sync): apply upstream changes (2026-09-11)` and
`b3db311b feat(sync): apply upstream changes (2026-09-12)`:

- `src/core/database/migrations/20260903104806_kilocode_board_reset.ts`
- `src/core/database/migrations/20260907102000_model_usage_index.ts`

These join the pre-existing `20260828074139_kilocode_board.ts` in
the same directory. This is the ONLY use of a
`src/core/database/migrations/` directory in the codebase (distinct
from the also-orphan `src/core/migration/` -- F6 -- which contains
a mix of `.ts`, `.sql`, and `.json` files that no loader
references).

Consumer scan: no runtime loader references either directory
(`grep -rEln "core/database/migrations|core/migration/" src/
--include='*.ts'` returns zero real hits outside self-references).
The situation is now:

- `src/core/migration/` -- seven files, tenth review at `block`
  (F6).
- `src/core/database/migrations/` -- three files, first review
  at `warn`.

Both are database-migration-shaped directories with no loader.
Very likely they should be a single directory once a loader lands.

Layering compliance: internal to `src/core/`, no rule violation.
Same shape as F6.

Severity `warn`. Disposition folds into Recommendation 6 / F6:
ADR 004 must cover BOTH directories, or delete BOTH. Owner:
`role-engineering` (loader landing) or `role-architecture`
(subtract).

### F36 `info` -- NEW USE-SITE landed: linkify (no action)

`src/cli/tui/utils/linkify.ts` (from PR #1695, 2026-09-10) landed
alongside its wire-in at `src/cli/tui/components/ToolRow.tsx:15`.
This is exactly the F31/F32/F33-shape file BUT wired in the same
PR that introduced it, which is the correct pattern. Recording
here to demonstrate the guardrail contract works when authors
follow it -- and to serve as the reference example for
Recommendation 3.

### F37..F42 `info` -- NEW USE-SITES landed for previously zero-inbound modules

Six modules that would have been F-warns if scanned in isolation
already have wiring landed:

- **F37**: `src/core/loopDetector.ts` <-
  `src/core/agenticChat.ts:41` (PR #1696).
- **F38**: `src/core/mistakeTracker.ts` <-
  `src/core/agenticChat.ts:42` (PR #1696).
- **F39**: `src/cli/utils/mistakeLimitPrompt.ts` <-
  `src/cli/commands/agent.ts:26` (PR #1696).
- **F40**: `src/core/inlineModelOverride.ts` <-
  `src/core/orchestrator.ts:15` +
  `src/core/streamingOrchestrator.ts:30` (PR #1718).
- **F41**: `src/tool/tools/open-plan.ts` <-
  `src/tool/tools/index.ts:45,109,173` (2026-08 sync).
- **F42**: `src/providers/model-selection.ts` <-
  `src/providers/index.ts:78` + three tool consumers
  (`src/tool/tools/task.ts:32`,
  `src/tool/tools/agent-manager.ts:8`,
  `src/tool/tools/agent-manager-models.ts:29` via
  `src/tool/model-selection.ts` re-export -- note: two files
  named `model-selection.ts` exist, one under `src/tool/` and one
  under `src/providers/`; not a violation, they cover different
  scopes -- see F19 heterogeneity note).

Additionally, `src/utils/tracing.ts` (PR #1712) is wired from
`src/cli/program.ts:15` and `src/providers/sapOrchestration.ts:66`
in the same PR.

No action for F36..F42 -- they are recorded as the *positive*
counterpart to F31..F35 (and to the "wire OR delete this cycle"
disposition for F26 / F29). Also inform Recommendation 3: the
sync-allowlist guardrail should exempt files whose landing diff
also introduces at least one consuming import elsewhere in `src/`.

## Recommendations

Priority order for the next fortnight (mostly carried forward;
new items for F31..F35):

1. **Wire OR delete F26 (pty), F29 (gitlab), F31 (kilocode/board),
   F32 (kilocode/pty/latch), F33 (bedrock-model-id), F35 (dual
   migrations) THIS CYCLE OR NEXT.** F26 and F29 already at `block`
   with expired deadlines. F31..F35 all at `warn` with 2026-09-21
   deadlines. Each is the same shape: a working module inside a
   legal layer with no local caller. `role-engineering` owns the
   wiring decision; `role-architecture` owns the subtract-if-unwired
   PR on 2026-09-21.

2. **File ADR 006 for `src/kilocode/` AND `src/core/kilocode/`.**
   Second cycle at `block` (F24). Now must cover:

   - `src/kilocode/sandbox/git.ts` (wired from
     `src/tool/tools/shell.ts:18`).
   - `src/kilocode/board/enabled.ts` (NEW, F31, zero-inbound).
   - `src/core/kilocode/zero-id.ts` (F27, zero-inbound).
   - `src/core/kilocode/fff.ts` (wired from
     `src/utils/filesystem.ts:8`).
   - `src/core/kilocode/pty/latch.ts` (NEW, F32, zero-inbound).

   Options: (a) charter `kilocode/` as an intentional
   upstream-parity boundary, ADR-record which files may live there
   and which must migrate; (b) rename both to `src/upstream/` or
   `src/vendor/upstream/`; (c) fold each file into its
   layer-appropriate home and delete both `kilocode/` directories.
   Rename `fff.ts` to a descriptive name in the same PR. Owner:
   this vertical. Scope: `docs(core)`.

3. **Sync-allowlist guardrail (`role-infrastructure`).** **NINTH
   consecutive review requesting this.** F31..F35 in this cycle
   alone are FIVE hits it would have caught. If `role-infrastructure`
   does not pick this up by `REVIEW-2026-09-21`, this vertical will
   file it directly as a small `.github/workflows/sync-allowlist.yml`
   composite (grep + fail exit, no runtime cost).

   Contract carried forward from `REVIEW-2026-09-07` Recommendation 3
   with the F36..F42-motivated exemption clause added:

   Fail sync PRs that introduce ANY of:

   - A new top-level `src/<name>/` directory not accompanied by
     `docs/adr/NNN-*.md` in the same diff.
   - A NEW **sub-directory** under an existing `src/<name>/`
     containing only files with zero incoming references from
     outside that directory, and not test files.
   - A **new file** inside an existing `src/<name>/` directory
     with zero incoming references from outside that directory,
     not a test, larger than 20 lines of executable code.

     **Exemption clause (new this cycle, motivated by F36..F42):**
     if the SAME PR that adds `foo.ts` also adds at least one
     `import ... from '.../foo.js'` line elsewhere under `src/`,
     the file is considered wired-at-landing and the rule passes.

   - Files matching `payment*`, `billing*`, `*credentials*`,
     `*stripe*`, `*catalog*`.
   - Empty-on-purpose files: size < 200 bytes AND consisting only
     of blank lines and single-line `//` comments (outside
     `docs/`, `.github/`, `.specify/`).
   - Any file with extension `.kt`, `.kts`, `.java`, `.swift`,
     `.rb`.
   - New file with an opaque three-or-fewer-letter basename
     (`fff.ts`, `zzz.ts`).

4. **Combined subtractive cleanup PR -- `chore(sync)`.** Updated
   this cycle to execute F22 / F23 (second deadline slip), stage
   F25 / F26 / F29 for immediate delete (all now at `block` with
   deadlines expired or expiring today), and F31..F35 for the
   2026-09-21 secondary deadline:

   ```sh
   # F10 / F11 / F13 (all seventh review, zero refs)
   git rm src/core/billing.ts
   git rm src/core/catalog.ts
   git rm src/session/data.ts
   git rm -r src/ui   # timers.ts + styles/composer.css

   # F16 -- 12 comment-only shims (ninth review)
   git rm src/tool/apply-patch.ts src/tool/application-tools.ts \
          src/tool/bash.ts src/tool/builtins.ts src/tool/edit.ts \
          src/tool/glob.ts src/tool/native.ts \
          src/tool/notebook.ts src/tool/question.ts \
          src/tool/read-filesystem.ts \
          src/tool/todowrite.ts src/tool/warpgrep.ts

   # F22 -- auto-delete deadline slipped TWO cycles, execute
   git rm src/session/reminders.ts src/session/ask-code-switch.txt

   # F23 -- auto-delete deadline slipped TWO cycles, execute
   git rm src/tool/grep.ts src/tool/grep-signal-controls.ts

   # F25 -- escalation clause fired this cycle
   git rm -r src/core/session

   # F26 -- second cycle at block, disposition deadline TODAY
   git rm -r src/core/pty

   # F29 -- second cycle, escalated to block per commitment
   git rm src/providers/gitlab.ts \
          src/providers/__tests__/gitlab.test.ts

   # F31..F33 / F35 -- if still unwired at 2026-09-21
   # git rm -r src/kilocode/board
   # git rm -r src/core/kilocode/pty
   # git rm src/providers/bedrock-model-id.ts
   # git rm -r src/core/database/migrations   # or merge into F6

   # F27 -- pending ADR 006 verdict
   # git rm src/core/kilocode/zero-id.ts
   ```

   Verify with `npm run typecheck && npm run lint &&
   npm run format:check && npm run test:coverage && npm run build`.
   F21 `undo/`, `update/`, `share/`, `sync/` folded into a follow-up
   PR (each is its own directory tree -- separate commit for
   reviewability).

5. **Fix F20 -- fifth cycle at `block`.** Same two options as last
   cycle. This is now the longest-running rule-4 violation in the
   series. Owner: `role-engineering`.

6. **Close F17 / F18 (unchanged).** One PR scoped
   `refactor(tools)` or two paired PRs. Ninth review of each.

7. **F21 subtractive PR (still `info`).** Fold into Recommendation
   4 follow-up.

8. **F22 / F23 -- execute the delete THIS CYCLE.** Second slipped
   deadline. Sixth review at `block` next cycle if not done. Land
   as part of Recommendation 4 within the fortnight.

9. **F2 / F3 (unchanged).** Migrate `src/cli/commands/models.ts:6`
   off `@sap-ai-sdk/ai-api`. Ninth review.

10. **F6 -- migration directory decision (unchanged, now with F35
    partner).** Tenth review of F6. ADR-004 must cover BOTH
    `src/core/migration/` AND `src/core/database/migrations/` (F35).
    Either land a loader for one and consolidate the other into
    it, or delete both.

11. **ADR 002 baseline -- deferred.** File AFTER Recommendation 4
    lands AND F22/F23/F25/F26/F29 physically delete AND F24/F27
    resolve via ADR 006.

## ADR needed?

**Yes -- ADR 006 for `src/kilocode/` AND `src/core/kilocode/`
(see Recommendation 2).** Second cycle at `block`. Now must cover
five files across three directories (see F24 above -- surface
grew this cycle via F31 and F32).

**Yes -- ADR 004 for the dual migration directory situation
(F6 + F35).** Previously `warn`-and-defer; now with a second
migration directory landed this cycle, the "one loader, one
directory" question becomes structural rather than housekeeping.

Potential future ADRs (unchanged from last cycle):

- ADR 002 -- baseline (after cleanup lands).
- ADR 003 -- `permission/ -> tool/registry.ts` carve-out
  (if F18 fix chooses that path over inversion).
- ADR 005 -- `cli/commands/models.ts` `@sap-ai-sdk/ai-api`
  exception or refactor (F2).

Sync-channel rule tripping tally this cycle:

- Rule 3 at **top-level** (new `src/<name>/`): **0 hits.**
- Rule 3 at **sub-directory level** (new sub-dir under existing
  `src/<name>/` with zero external refs, not tests): **3 hits**
  -- `src/providers/protocols/` (F30 already open),
  `src/kilocode/board/` (F31), `src/core/kilocode/pty/` (F32).
  Reversal of last cycle's zero-sub-dir result.
- Rule 3 at **file level** (new file inside existing dir, zero
  incoming refs from outside that dir, not test, >20 LoC):
  **5 hits** -- `kilocode/board/enabled.ts` (F31),
  `core/kilocode/pty/latch.ts` (F32),
  `providers/bedrock-model-id.ts` (F33),
  `providers/protocols/shared.ts` (F34/F30),
  `core/database/migrations/*.ts` (F35, two files).
- Rule 4 (upward import): F20 survives at `block` for a fifth
  consecutive cycle. F17 / F18 unchanged. **Zero NEW rule-4 hits.**

Direction of travel is **negative** this cycle. Zero new top-level
directories is genuine progress (third clean cycle), and rule-4
remains flat. F36..F42 also show that when authors follow the
"wire-in the same PR" pattern the guardrail is trivially
satisfied. But the sync-channel added FIVE new zero-inbound files
and THREE new zero-inbound sub-directories in one week -- exactly
the pattern Recommendation 3 has been asking to gate for nine
consecutive cycles. The gap between deferred-cleanup and
being-ignored has narrowed further; if `REVIEW-2026-09-21` shows
F22/F23 still present AND F25/F26/F29 still present AND F24 still
without ADR 006, this vertical files both Recommendation 3
(the guardrail workflow) and Recommendation 4 (the subtractive
PR) directly rather than requesting them from other roles.
