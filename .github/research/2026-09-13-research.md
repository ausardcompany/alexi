# Research - 2026-09-13

Focus: **all** (CLI, providers, tools, MCP, commands, hooks, skills, plugins)

> Sunday, `date -u +%u` = 7, so **daily repos only** (Kilocode, Cline, OpenCode). Weekly repos skipped per schedule (not Monday). Today's scan: commits are identical to 2026-09-12 (no new commits since Friday's research cycle). Kilocode shows same 5 PRs (#14057 docs cleanup, #14045 compaction model setting, #14046 working indicator spacing, #13192 auxiliary routing, #14013 Kilo Swarm env flag — which appeared in v7.6.2 release); Cline shows same 20 PRs (Crusoe provider, native Switch, AI SDK OTLP trace relay #13974, cloud session foundations, voice error surfacing, OpenCode Go routing, desktop bug fixes); OpenCode shows same 17 commits (session options restore, batch workspace endpoints, DeepSeek V4.1 Flash docs, Go subscription fixes, model catalog updates).

## Executive Summary

- **0 actionable items** today (Sun 2026-09-13): No new commits since 2026-09-12. All commits in today's scan are identical to Friday's research. The single actionable item from 2026-09-12 (Cline #13974 AI SDK OTLP trace relay MODERATE ~4h) is **ALREADY IMPLEMENTED** in Alexi — full OpenTelemetry tracing infrastructure exists in `src/utils/tracing.ts` (355 lines) with all OTLP exporters (grpc/http-json/http-protobuf), privacy-preserving defaults (metadata-only, content recording opt-in), sampling (`ALEXI_TRACE_SAMPLE_PERCENT`), opt-out support, and session-deterministic hashing. Dependencies installed: `@opentelemetry/api` ^1.9.1, `@opentelemetry/sdk-trace-node` ^2.11.0, `@opentelemetry/exporter-trace-otlp-grpc` ^0.222.0, `@opentelemetry/exporter-trace-otlp-http` ^0.222.0, `@opentelemetry/exporter-trace-otlp-proto` ^0.222.0. Implementation matches Cline PR #13974 pattern. No gaps.
- **Rotation status**: Kilocode daily (dryness 2/5 — no NEW CLI-actionable items today; same commits as Friday). Cline daily (dryness 1/5 — same commits as Friday; the 1 CLI-portable item from 2026-09-12, AI SDK OTLP trace relay, is ALREADY DONE in Alexi). OpenCode daily (dryness 19/5 — **EXCEEDS 5/5 threshold**, 19th consecutive cycle with zero NEW CLI-actionable items beyond routine model catalog updates, demotion to weekly URGENT per 2026-09-06 through 2026-09-12 recommendations).
- **0 open auto-implement issues** (per `gh issue list --label auto-implement`). Issue #1698 Kilo Swarm env flag (carry-forward from 2026-09-11) appears to have been resolved or closed between Friday and Sunday. Engineering pipeline clear.
- **Carry-forward from previous days**: 45 items from 2026-09-11 and earlier (top 3 from 2026-09-11: Kilocode #14006 inline model referencing SMALL ~2h, Kilocode #14013 Kilo Swarm env flag SMALL ~1h — status unknown, possibly resolved; top 3 from 2026-09-10: Cline #13815 nested PowerShell script preservation MODERATE ~3h, Kilocode #13990 board_post recipient state warning SMALL ~2h, OpenCode #48270 DeepSeek V4.1 Flash model SMALL ~1h; top 3 from 2026-09-09: Kilocode #13944 clickable terminal links SMALL ~2h, Kilocode #13969 mistake-limit prompt MODERATE ~4h, home-directory guard SMALL ~1h — DONE; top 2 from 2026-09-02: API key sanitization + auth error classification HIGH ~6h, abort propagation HIGH ~6h; plus 38 older). Total backlog ~93h cumulative (reduced by ~1h if #1698 resolved), top 5 ~13h.

## Detailed Findings

#### 1. No new commits since 2026-09-12 (Sunday weekend lull)

- **What**: All three daily repos (Kilocode, Cline, OpenCode) show identical commit histories to 2026-09-12 Friday research. GitHub API returned the same 20 commits for each repo. No development activity occurred Saturday or Sunday (weekend).
- **Why**: Expected weekend pattern for open-source projects. Friday's commits (#14057, #14045, #14046, #13192, #14013 for Kilocode; #12267, #14073, #13974, #13558, #14071, #14048, #14043, #14058, #14053, #14052, #14025, #14004, #14050, #14040, #14003, #14039, #13851, #14035, #14012 for Cline; #48225, #48491, #48363, #48309, #48280, #48270, #48269, #48192, #48123, #48130, #48057, #34441 for OpenCode) remain the most recent. Nothing new to analyze.
- **Source**: https://github.com/Kilo-Org/kilocode/commits, https://github.com/cline/cline/commits, https://github.com/sst/opencode/commits (API fetch timestamp: 2026-09-13 09:18 UTC)
- **Category**: N/A (no new items)
- **Size**: LOW priority, SKIP. No new work.

#### 2. Cline #13974 AI SDK OTLP trace relay — ALREADY IMPLEMENTED in Alexi (LOW priority, duplicate)

- **What**: Friday's research (2026-09-12) identified Cline #13974 (AI SDK OTLP trace relay) as the single actionable item (MODERATE ~4h). Investigation today reveals Alexi ALREADY HAS complete OpenTelemetry tracing infrastructure: (1) `src/utils/tracing.ts` (355 lines, full OTLP export with grpc/http-json/http-protobuf support, privacy-preserving defaults, sampling, opt-out), (2) All required dependencies installed (`@opentelemetry/api` ^1.9.1, `@opentelemetry/sdk-trace-node` ^2.11.0, `@opentelemetry/exporter-trace-otlp-grpc` ^0.222.0, `@opentelemetry/exporter-trace-otlp-http` ^0.222.0, `@opentelemetry/exporter-trace-otlp-proto` ^0.222.0), (3) Configuration via `ALEXI_OTEL_TRACES_EXPORTER`, `ALEXI_OTEL_EXPORTER_OTLP_ENDPOINT`, `ALEXI_TRACE_SAMPLE_PERCENT`, `ALEXI_TRACE_RECORD_CONTENT`, (4) Telemetry opt-out support (`telemetryOptOut` in `~/.alexi/config.json`), (5) Session-deterministic sampling (whole sessions sample together), (6) Metadata-only by default (no prompt/completion content unless `ALEXI_TRACE_RECORD_CONTENT=true`), (7) Fail-closed on config read errors. Implementation architecture matches Cline PR #13974 pattern exactly (reference comment in `src/utils/tracing.ts:20-21`). Provider spans emitted from `src/providers/sapOrchestration.ts`. No gaps, no missing pieces.
- **Why**: The feature Friday's research recommended implementing already exists. Code archaeology shows it was implemented prior to this research cycle. The AGENTS.md file (lines 1-118) documents this extensively but was not consulted before Friday's brief. The duplicate recommendation wasted analysis time and would have wasted engineering time if not caught in this cycle.
- **Source**: https://github.com/cline/cline/pull/13974 (reference implementation), /home/runner/work/alexi/alexi/src/utils/tracing.ts (Alexi implementation), /home/runner/work/alexi/alexi/package.json lines 36-42 (dependencies)
- **Category**: provider (observability, telemetry, OTLP, OpenTelemetry) — DUPLICATE
- **Size**: LOW priority, SKIP. Already done. Remove from recommendations and mark dryness cycle accordingly.

#### 3. All other Friday items remain unchanged (LOW priority, carry-forward)

- **What**: The remaining 19 Kilocode items (VS Code UI housekeeping #14057/#14045/#14046, kilo-credentials-specific auxiliary routing #13192, Kilo Swarm env flag #14013 — which may have been resolved as issue #1698 is no longer open), 19 Cline items (Crusoe provider #12267, native Switch #14073, cloud session foundations #13558, voice error surfacing #14071, OpenCode Go routing #14048, desktop bug fixes #14053/#14052/#14025/#14004/#14050/#14040/#14003/#14039/#13851/#14035/#14012, Cline Pass picker #14058, dark mode eye color #14043), and 17 OpenCode items (session options restore #48225, batch endpoints #48491, DeepSeek docs #48363, Go subscription fixes #48309, Omen Alpha removal #48280, DeepSeek V4.1 Flash #48270 — carry-forward from 2026-09-10, model catalog updates) are all identical to 2026-09-12 analysis. None are new CLI-portable features for Alexi. All were analyzed in Friday's brief as LOW priority SKIP or carry-forward from earlier cycles.
- **Why**: Weekend: no development activity. Friday's analysis remains current.
- **Source**: See 2026-09-12 research detailed findings #2-11 (same commit hashes, same reasoning)
- **Category**: N/A (no new items)
- **Size**: LOW priority, SKIP. Refer to 2026-09-12 for full analysis.

## Actionable Recommendations

1. **0 NEW items** today (Sun 2026-09-13). Plus carry-forward from 2026-09-11: (1) Kilocode #14006 inline model referencing (SMALL ~2h), (2) Kilocode #14013 Kilo Swarm env flag (SMALL ~1h) — status unknown, issue #1698 no longer open, possibly resolved; plus carry-forward from 2026-09-10: (3) Cline #13815 nested PowerShell script preservation (MODERATE ~3h), (4) Kilocode #13990 board_post recipient state warning (SMALL ~2h), (5) OpenCode #48270 DeepSeek V4.1 Flash model (SMALL ~1h).
2. **Cline #13974 AI SDK OTLP trace relay**: ALREADY IMPLEMENTED. Remove from recommendations. Alexi has full OpenTelemetry tracing infrastructure in `src/utils/tracing.ts` with all required dependencies, OTLP exporters (grpc/http-json/http-protobuf), privacy-preserving defaults, sampling, opt-out support, session-deterministic hashing, and provider span emission. Implementation matches Cline PR #13974 pattern. No work needed.
3. **Inline model referencing (carry-forward from 2026-09-11)**: Add prompt-inline model override to src/core/orchestrator.ts and src/core/streamingOrchestrator.ts. Before routing a turn, scan the user message text for `@<provider>/<model>` patterns (regex `@([a-z0-9-]+)/([a-z0-9-./]+)`), extract the model ID, validate against the live model catalog (src/providers/modelCatalog.ts), and if found, override the route/model for that turn only (leave session default unchanged). Store the override in turn context so src/core/router.ts uses it. Test: `ax chat -m "test default" -m "@anthropic/claude-opus-4 explain bubble sort"` should use default for turn 1 and opus for turn 2. Also test invalid references (`@nonexistent/model`) and confirm they are ignored or surface a helpful error. Touches src/core/orchestrator.ts, src/core/streamingOrchestrator.ts, src/core/router.ts.
4. **Kilo Swarm env flag (carry-forward from 2026-09-11, issue #1698 status UNKNOWN)**: Issue #1698 no longer appears in `gh issue list --label auto-implement` output. Either resolved between 2026-09-12 and 2026-09-13, or removed/closed/relabeled. If still needed, the recommendation remains: Add env flag enable path to src/tool/tools/board.ts. Check `process.env.KILO_EXPERIMENTAL_SHARED_AGENT_BOARD === '1'` or `process.env.KILO_EXPERIMENTAL === '1'` in addition to the config key. Follow Kilocode's OR rule: board is enabled when config true OR flag set. Extract the check into a helper (e.g. `isBoardEnabled()`) so the rule lives in one place. Test: remove config key (`ax config delete experimental.sharedAgentBoard`), set `export KILO_EXPERIMENTAL_SHARED_AGENT_BOARD=1`, run `ax chat`, confirm board tools (`kilo_board_read`, `kilo_board_write`) are registered. Also test umbrella flag (`KILO_EXPERIMENTAL=1`). Touches src/tool/tools/board.ts, possibly src/config/index.ts. Verify issue status before scheduling.
5. **Nested PowerShell script preservation (carry-forward from 2026-09-10)**: Port Cline #13815's unwrapping logic to src/tool/tools/bash.ts. Add a helper that detects standalone `powershell`/`pwsh` with `-NoProfile` and one ASCII single- or double-quoted `-Command` body, decodes the script text (strip outer quotes, unescape inner quotes), and returns the decoded script + requested executable. Wire into the bash tool's PowerShell execution path so the bootstrap receives the unwrapped script. Test: `ax chat -m "run: powershell -Command \"Get-ChildItem | Where-Object { $_.Name -like '*.ts' }\""` on Windows should succeed without "property not found" errors. Verify cancellation still works and non-matching invocations pass through unchanged.
6. **board_post recipient state warning (carry-forward from 2026-09-10)**: Add `kilo_board_post` tool to src/tool/tools/board.ts with schema `{ recipient: string, content: string }`. Look up recipient session via SessionManager, check status (running/completed/failed/cancelled/unknown), return `{ messageId, boardId, availability: { recipientState } }`. If stopped, add warning: "Recipient has already stopped; message stored but no reply should be expected." Test by creating a swarm with one child that completes, then having parent post to that child — confirm warning appears and parent does not resume.
7. **DeepSeek V4.1 Flash model (carry-forward from 2026-09-10)**: Add `deepseek-v4.1-flash` to src/providers/modelCatalog.ts static model list with same reasoning capability profile as `deepseek-v4-chat` (reasoning: levels, budget: reasoning_effort). Test: `ax chat --model deepseek-v4.1-flash -m "test"` should succeed (requires SAP AI Core deployment; mock if unavailable). Verify reasoning: `ax chat --model deepseek-v4.1-flash --reasoning-effort high -m "explain bubble sort"` should emit reasoning blocks.
8. **Rotation — URGENT**: OpenCode daily → WEEKLY (Mon only). Dryness 19/5 (19th consecutive cycle, well past the 5/5 threshold per 2026-09-06 through 2026-09-12). Update `.github/prompts/agent1-research-system.md` to mark OpenCode as weekly (Mon only). Kilocode dryness 2/5 (no NEW CLI-actionable items today; same commits as Friday, all VS Code UI or kilo-credentials-specific). Cline dryness 1/5 (same commits as Friday; the 1 CLI-portable item from 2026-09-12, AI SDK OTLP trace relay, is ALREADY DONE in Alexi, so no NEW actionable items).
9. **0 open auto-implement issues** (per `gh issue list --label auto-implement`). Issue #1698 Kilo Swarm env flag (carry-forward from 2026-09-11) no longer appears — status unknown (resolved/closed/relabeled between 2026-09-12 and 2026-09-13). Engineering pipeline clear. Agent 2 should verify #1698 status and, if still needed, recreate with current context. Otherwise, queue carry-forward from 2026-09-11 (inline model referencing ~2h) plus carry-forward from 2026-09-10 (nested PowerShell ~3h, board_post ~2h, DeepSeek ~1h).
10. **Carry-forward from previous days**: 45 items from 2026-09-11 and earlier (top 3 from 2026-09-11: Kilocode #14006 inline model referencing SMALL ~2h, Kilocode #14013 Kilo Swarm env flag SMALL ~1h — issue #1698 status unknown; top 3 from 2026-09-10: Cline #13815 nested PowerShell script preservation MODERATE ~3h, Kilocode #13990 board_post recipient state warning SMALL ~2h, OpenCode #48270 DeepSeek V4.1 Flash model SMALL ~1h; top 3 from 2026-09-09: Kilocode #13944 clickable terminal links SMALL ~2h, Kilocode #13969 mistake-limit prompt MODERATE ~4h, home-directory guard SMALL ~1h — DONE; top 2 from 2026-09-02: API key sanitization + auth error classification HIGH ~6h, abort propagation HIGH ~6h; plus 38 older). Total backlog ~93h cumulative (reduced by ~4h if AI SDK OTLP trace relay ALREADY DONE removed, and by ~1h if #1698 resolved — net ~88h). Agent 2 should prioritize 2026-09-11's 1 carry-forward (~2h inline model referencing, verify #1698 status) + 2026-09-10's 3 carry-forward (~6h) + 2026-09-09 clickable links (~2h) + 2026-09-02's 2 HIGH items (~12h) as immediate auto-implement queue candidates (~22h total top-priority work).
11. **Future radar**: (a) Kilocode #13927 /caffeinate command (defer until user demand), (b) Kilocode #13658 + #13657 Agent Plugin management via Hub (defer until Alexi's plugin adoption increases), (c) Kilocode SDK #13519 authenticated remote Hub connections (defer until Alexi adds a Hub), (d) Kilocode #13964 imported session warnings + compaction (defer until Alexi adds session import support), (e) Kilocode #13817 run_commands background-child pipe handling (MODERATE ~3-4h, defer to dedicated investigation per 2026-09-08, 2026-09-09, 2026-09-10, 2026-09-11, 2026-09-12).
12. **Process improvement**: The duplicate recommendation for Cline #13974 AI SDK OTLP trace relay (MODERATE ~4h, recommended in 2026-09-12, discovered ALREADY IMPLEMENTED today) wasted analysis time and would have wasted engineering time. Root cause: insufficient codebase awareness before making recommendations. Mitigation: (1) Always check `src/` for existing implementations before recommending a feature, (2) Consult AGENTS.md which documents existing OpenTelemetry infrastructure (lines 1-118), (3) Use the explore agent to verify feature gaps before declaring them actionable. This incident reduces confidence in Friday's brief — all recommendations should be re-verified for similar duplicates. Agent 2 should cross-check carry-forward items against current codebase state before creating issues.

(End of file)
