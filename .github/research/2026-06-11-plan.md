# Planning Brief - 2026-06-11

Source research: `.github/research/2026-06-11-research.md`

## Context

- Open `auto-implement` labelled issues at start of cycle: **0**
  (the `enhancement`-labelled backlog of 9 items is not in the
  engineering pickup queue; the `auto-implement` queue is empty).
- Research surfaced **4** mechanically transferable, small (<= 3h)
  items from upstream commits in the last 24h. All 4 verified against
  current `src/` tree and translated into self-contained issues below.
- Engineering-agent stall is at 5 cycles. Smallest-PR ladder applies:
  prefer the two `~2h` items (#4, #2) first.

## Plan -> Tasks -> Issues

| # | Title                                                           | Cat   | Size      | Files                                                                                                       | Issue |
| - | --------------------------------------------------------------- | ----- | --------- | ----------------------------------------------------------------------------------------------------------- | ----- |
| 1 | `KILO_COMMAND_TIMEOUT_MAX_MS` cap on bash/shell timeouts        | tool  | small ~3h | `src/tool/shell/timeoutPolicy.ts` (new), `src/tool/tools/bash.ts`, `src/tool/tools/shell.ts`, tests          | #754  |
| 2 | `windowsHide: true` on every spawn / execFile                   | tool  | small ~2h | `src/tool/tools/{background-process,repo-clone,grep,diagnostics}.ts`, `src/utils/{clipboard,githubPr}.ts`, lint test | #755  |
| 3 | MCP `notifications/message` -> logger forwarding                | mcp   | small ~3h | `src/mcp/client.ts`, `src/mcp/__tests__/notifications.test.ts` (new)                                        | #756  |
| 4 | MCP catalog pagination AbortSignal + timeout                    | mcp   | small ~2h | `src/mcp/client.ts`, `src/mcp/__tests__/catalog-timeout.test.ts` (new)                                      | #757  |

## Priority order (smallest-PR ladder)

1. Item 4 (`~2h`, isolated to `client.ts:68-83` + new test file)
2. Item 2 (`~2h`, mechanical sweep with regression-prevention lint test)
3. Item 1 (`~3h`, unblocks open issue #710 / TimeoutError adoption)
4. Item 3 (`~3h`, isolated to `client.ts` connect path)

## Out-of-scope (no-action) signals from research

- cline #11372 singleton Cline Hub - presupposes daemon we lack.
- opencode #31837 / #31822 connector auth + v2 session API -
  platform-level, not portable.
- kilocode #11094 transcript virtualization - VS-Code-only, not Ink.

## Recommendations forwarded to other roles

- `role-management`: file a status issue if `auto-implement-issues-
  closed-7d == 0` for 3 consecutive days (engineering-agent stall).
- `role-management` / Daily Merge: confirm reference-repo rotation -
  drop `continuedev`, add `sst/opencode` (validated by 2/4 actionable
  items today).
