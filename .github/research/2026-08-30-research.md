# Research - 2026-08-30

Focus: **all** (CLI, providers, tools, MCP, commands, hooks, skills, plugins)

> Sunday, `date -u +%u` = 7, so **daily repos only** (Kilocode, Cline, OpenCode). Weekly/monthly repos poll Mon only. Today's scan: Kilocode JetBrains worktree run configs (#13278 carry-forward from 2026-08-29), session sharing menu (#13552 carry-forward), prompt actions menu (#13556 carry-forward), inline code linkify (#13551 carry-forward), fork runner fallback (#13364 carry-forward), worktree removal probe skip (#13525 carry-forward), worktree launch bundle check (#13559 carry-forward); Cline hook spawn cwd validation (#13422 CRITICAL security/stability fix, carry-forward from 2026-08-29 HIGH), Langfuse tracer detection minify survivability (#13680 CRITICAL minify pattern, carry-forward from 2026-08-29 MODERATE), hook task id threading (#13547 hook telemetry regression fix), undici/mermaid/js-yaml bumps (#13675 carry-forward), UI attachment drop zone (#13672 carry-forward), refresh models (#13670 carry-forward), session search timeout coverage (#13642 carry-forward), teammate abort propagation (#13647 carry-forward), agent plugins discovery (#13017 carry-forward), desktop tool images (#13643 carry-forward), e2e teardown fixes (#13646 carry-forward), GitHub integration onboarding (#13225 carry-forward), remote MCP connect timeout (#13639 carry-forward), session history FTS (#13420 carry-forward), global rules path discovery (#13614 carry-forward), hub-managed schedule persistence (#13627 carry-forward), AI SDK 7 Langfuse telemetry (#13651 telemetry upgrade), CLI search fallback (#13644 E2E test harness); OpenCode Hy3 docs removal + Go chart rendering (#46221, #46213), nix hashes (#multiple), Go usage animations (#46055), model docs (Ling 3.0, Hy4, Qwen3.8), Azure auth Bun removal (#45845 carry-forward), config JSON comparison (#45784 carry-forward), Azure CLI auth (#45079 carry-forward), Bedrock reasoning caching (#45769 carry-forward), v2 config in v1 (#45421 carry-forward), Go comparison mobile (#45044), ox alpha GLM mapping (#45542), duplicate usage row merge (#45503).

## Executive Summary

- **Zero NEW actionable items today** (all CLI-portable commits are carry-forwards from 2026-08-29). Cline #13422 hook spawn cwd validation remains CRITICAL HIGH priority (security/stability fix). Cline #13680 Langfuse minify detection remains defensive pattern for telemetry.
- **Cline #13422 hook spawn cwd validation (CRITICAL HIGH ~6h from 2026-08-29)**: Hook spawn failure should fail the hook, not crash the orchestrator. Nonexistent cwd should reject with descriptive error, not relocate to process.cwd() and let hook read/write wrong location. Security fix. Alexi has NO hooks implementation yet (src/core/ has no hooks/, src/hooks/ does not exist), but pattern is critical for when hooks are added.
- **Cline #13680 Langfuse minify detection (CRITICAL MODERATE ~4h from 2026-08-29)**: Constructor-name checks break when minifier renames classes. Use structural checks (getDelegate method, lifecycle methods, object identity) instead. Alexi has NO Langfuse integration, but pattern applies to ANY telemetry integration (OpenTelemetry, Sentry, Datadog).
- **Cline #13547 hook task id threading (LOW ~2h)**: Cline threaded task id into hook runner creation so execution telemetry fires. SDK hooks adapter created every hook runner without task id, and StdioHookRunner gates all captureHookExecution calls on one being set — next variant emitted zero hooks.execution events while discovery telemetry fired normally. Pass task id (and tool name for tool hooks) at all five factory.create call sites. Alexi has NO hooks system (no src/hooks/, no hookExecutor.ts), so not actionable. Defer until hooks prioritized.
- **Cline #13651 AI SDK 7 Langfuse telemetry (LOW ~3h)**: Cline upgraded to AI SDK 7 Langfuse telemetry integration. Replace SDK 6 telemetry wrapper with SDK 7's built-in Langfuse support. Alexi has NO Langfuse integration (no Langfuse imports in src/), not actionable. Defer until Langfuse integration prioritized.
- **Other items**: Kilocode JetBrains features (all carry-forwards from 2026-08-29, UI-only SKIP), Cline desktop/UI features (all carry-forwards, Alexi is CLI SKIP), Cline multi-agent/plugins (carry-forwards, SKIP), Cline test improvements (carry-forwards, test infrastructure SKIP), OpenCode SaaS/docs (all maintenance/informational SKIP).
- **Rotation**: Kilocode daily (zero actionable today, dryness +1 = 1/5 since 2026-08-29 reset), Cline daily (zero NEW actionable today, dryness +1 = 1/5 since 2026-08-29 reset — two critical carry-forwards from 2026-08-29 remain HIGH priority), OpenCode daily (zero CLI-portable commits today, dryness +1 = 5/5 since 2026-08-24 demotion — **THRESHOLD REACHED: propose demotion to weekly (Mon only) in next brief**).
- **Carry-forward from 2026-08-29**: 42 items (2026-08-29's #1 hook spawn cwd validation CRITICAL HIGH ~6h, 2026-08-29's #2 Langfuse minify detection CRITICAL MODERATE ~4h, 2026-08-28's #1 session history FTS HIGH ~10h, 2026-08-28's #2 remote MCP connect timeout MODERATE ~4h, 2026-08-28's #3 global rules path discovery MODERATE ~3h, 2026-08-27's #3 apply_patch CRLF preservation MODERATE ~5h, 2026-08-19's #1 streaming command output HIGH ~12h, 2026-08-15's #1 image generation HIGH ~8h, 2026-08-20's #1 file mention quoting MODERATE ~5h, 2026-08-21's #1 tool hook context injection MODERATE ~8h, plus 32 others).

## Detailed Findings

#### 1. Cline #13422: Hook spawn cwd validation — CRITICAL security/stability fix (CRITICAL HIGH ~6h from 2026-08-29)

- **What**: Cline prevented hook spawn failures from crashing the entire cline-core process. Hook child-process spawn failure emitted "error" on HookProcess with no listener registered, which Node's EventEmitter turns into uncaught exception — killing the entire core process instead of failing the one hook. Guard: check listenerCount before emit so rejection (which StdioHookRunner handles) is the only propagation path. Trigger was workspace root that no longer exists passed as spawn cwd: Node reports nonexistent cwd as misleading ENOENT on launcher binary ("spawn /bin/sh ENOENT"). Validate cwd existence before spawning, fall back to no explicit cwd with warning. Updated: fail hooks with missing cwd instead of relocating. Running hook with missing cwd from host process's own cwd would let relative paths read/write unrelated location (e.g. IDE install directory). Reject before spawning with error naming missing directory; runner reports hook as failed, task continues. Carry pre-spawn failure messages into HookExecutionError details so cause is not reduced to bare "exited with code 1".
- **Why**: Hook spawn failure should fail the hook, not crash the orchestrator. Nonexistent cwd should reject with descriptive error, not relocate to process.cwd() and let hook read/write wrong location. Security fix.
- **Source**: https://github.com/cline/cline/pull/13422 (fix(vscode): prevent hook spawn failures from crashing the core process)
- **Category**: tool (hooks / spawn)
- **Size**: CRITICAL HIGH (~6h — Alexi has NO hooks implementation yet. src/hooks/ does not exist, src/core/ has no hooks/. Pattern is CRITICAL for when hooks are added: (1) validate cwd exists before spawn (fs.existsSync on cwd path, reject with descriptive error if missing), (2) guard "error" event emit behind listenerCount check (check emitter.listenerCount('error') > 0 before emit('error', err)), (3) fail with descriptive error instead of relocating to process.cwd() (no fallback cwd, descriptive rejection names missing directory), (4) carry pre-spawn failure details into hook execution error (wrap spawn error with context: hook name, missing cwd path, original error message). Security fix — hook running from wrong cwd is dangerous. Defer until hooks implementation prioritized, but remember pattern).

#### 2. Cline #13680: Langfuse tracer detection minify survivability — CRITICAL defensive pattern (CRITICAL MODERATE ~4h from 2026-08-29)

- **What**: Cline fixed Langfuse telemetry initialization to survive minified release builds. Release binaries compiled with minify enabled rename classes, so initializeLangfuseTelemetry's constructor-name guard never matched "ProxyTracerProvider" and silently returned readiness=false in production (dev unminified source worked). Replace constructor-name comparisons with checks that survive minification: detect proxy structurally via getDelegate, distinguish recording provider from no-op fallback by lifecycle methods, confirm NodeTracerProvider registration by object identity. When foreign provider owns global slot, attach Langfuse span processor if it accepts processors, else shut down orphaned provider and report rejection. Verified by bundling module with Bun minify:true against real OpenTelemetry packages: previous code reproduces readiness=false (provider class name mangles to "H2"), new code initializes with readiness=true.
- **Why**: Telemetry initialization should survive minified production builds. Constructor-name checks break when minifier renames classes.
- **Source**: https://github.com/cline/cline/pull/13680 (fix(llms): make Langfuse tracer detection survive minified release builds)
- **Category**: tool (telemetry / minify)
- **Size**: CRITICAL MODERATE (~4h — Alexi has NO Langfuse integration currently (no Langfuse imports in src/). Pattern is CRITICAL for ANY telemetry integration using constructor-name checks. Minification renames classes, so `instanceof ClassName` works but `obj.constructor.name === 'ClassName'` fails. If Alexi adds Langfuse (or OpenTelemetry, Sentry, Datadog, any telemetry with class-based initialization), use structural checks instead: (1) detect proxy via getDelegate method existence (`typeof provider.getDelegate === 'function'`), (2) distinguish recording provider from no-op by lifecycle methods (`typeof provider.shutdown === 'function'`), (3) confirm registration by object identity (`trace.getTracerProvider() === myProvider`), (4) when foreign provider owns global slot, attach processor if it accepts them (`typeof provider.addSpanProcessor === 'function'`). Defensive pattern for minified builds. Defer until Langfuse integration is prioritized, but remember pattern for any telemetry integration).

#### 3. Cline #13547: Hook task id threading — hook telemetry regression fix (LOW ~2h)

- **What**: Cline threaded task id into hook runner creation so execution telemetry fires. SDK hooks adapter created every hook runner without task id, and StdioHookRunner gates all captureHookExecution calls on one being set — so next variant emitted zero hooks.execution events while discovery telemetry fired normally. Pass task id (and tool name for tool hooks) at all five factory.create call sites, pin threading with regression test.
- **Why**: Hook execution telemetry should fire when task id is available. Missing task id silently disables hook execution telemetry.
- **Source**: https://github.com/cline/cline/pull/13547 (fix(vscode): thread task id into hook runner creation so execution telemetry fires)
- **Category**: tool (hooks / telemetry)
- **Size**: LOW (~2h — Alexi has NO hooks system (no src/hooks/, no hookExecutor.ts, no hook runner factory). Pattern is hooks telemetry threading: pass task id/tool name to hook runner constructor at every creation site. Not actionable until hooks implementation exists. Defer until hooks prioritized).

#### 4. Cline #13651: AI SDK 7 Langfuse telemetry — telemetry upgrade (LOW ~3h)

- **What**: Cline upgraded to AI SDK 7's built-in Langfuse telemetry integration. Replace SDK 6 telemetry wrapper with SDK 7's native Langfuse support. Test covers Langfuse runtime context propagation.
- **Why**: AI SDK 7 has first-class Langfuse telemetry support. Replace custom wrapper with built-in integration.
- **Source**: https://github.com/cline/cline/pull/13651 (fix(llms): use AI SDK 7 Langfuse telemetry)
- **Category**: provider (telemetry)
- **Size**: LOW (~3h — Alexi has NO Langfuse integration (no Langfuse imports in src/). Pattern is AI SDK 7 telemetry upgrade. Alexi uses SAP AI Core providers (src/providers/), not AI SDK. Not actionable. Defer until Langfuse integration prioritized).

#### 5. Cline #13644: CLI E2E external URL capture — test harness improvement (LOW/SKIP)

- **What**: Cline VS Code E2E tests now capture external URLs during E2E runs instead of opening real browsers. External URLs (OAuth callbacks, browser launches) opened real browsers under E2E_TEST, orphaning processes holding harness pipes on runner. Capture URL instead of spawning browser.
- **Why**: E2E tests should not spawn orphaned browser processes. External URL capture lets tests drive OAuth/browser flows without process leaks.
- **Source**: https://github.com/cline/cline/pull/13644 (test(vscode): prevent E2E worker teardown hangs)
- **Category**: tests (E2E)
- **Size**: LOW (Cline E2E test harness improvement. Test infrastructure, not CLI feature. SKIP).

#### 6. Kilocode, Cline, OpenCode carry-forwards: all items from 2026-08-29 research (all SKIP/carry-forward)

- **What**: All other commits today are carry-forwards from 2026-08-29: Kilocode JetBrains UI features (#13278, #13552, #13556, #13551, #13559, #13525, #13364 all UI/IDE plugin features), Cline dependency bumps (#13675), Cline desktop/UI features (#13672, #13670, #13643, #13225), Cline test improvements (#13642, #13646), Cline multi-agent/plugins (#13647, #13017), Cline remote MCP connect timeout (#13639), Cline session history FTS (#13420), Cline global rules path discovery (#13614), Cline hub schedule persistence (#13627), OpenCode docs/nix/SaaS features (all maintenance/informational).
- **Why**: Already analyzed in 2026-08-29 research. No new analysis needed.
- **Source**: See 2026-08-29 research detailed findings.
- **Category**: various
- **Size**: Various (all analyzed in 2026-08-29 brief).

## Actionable Recommendations

1. **Zero NEW actionable items today**. All CLI-portable commits are carry-forwards from 2026-08-29. Top priorities remain: Cline #13422 hook spawn cwd validation (CRITICAL HIGH ~6h, security/stability fix for when hooks are implemented), Cline #13680 Langfuse minify detection (CRITICAL MODERATE ~4h, defensive pattern for any telemetry integration).
2. **Top priority from carry-forward**: Cline #13422 hook spawn cwd validation (CRITICAL HIGH ~6h from 2026-08-29) — Alexi has NO hooks implementation yet (no src/hooks/, no src/core/hooks/), but pattern is CRITICAL for when hooks are added. Hook spawn failure should fail the hook, not crash orchestrator. Nonexistent cwd should reject with descriptive error, not relocate to process.cwd() and let hook read/write wrong location. Security fix. Implement guards when hooks system is prioritized: (1) validate cwd before spawn, (2) guard "error" emit behind listenerCount, (3) fail instead of relocating, (4) carry pre-spawn failure details into error.
3. **Rotation proposal**: OpenCode daily repo has reached 5/5 dryness threshold (zero CLI-portable commits for 5 consecutive cycles since 2026-08-24 demotion from weekly to daily trial). **PROPOSE DEMOTION: move OpenCode from daily to weekly (Mon only)** in next brief. Rationale: OpenCode commits are predominantly SaaS console features (Go usage charts, usage animations, docs updates, config v1/v2 migration) and provider-specific optimizations (Azure CLI auth, Bedrock reasoning caching) that are not CLI-portable to Alexi. Five consecutive dry cycles indicate low signal-to-noise ratio for daily polling. Weekly (Mon only) cadence reduces review overhead while preserving coverage of major feature launches. Kilocode daily (dryness 1/5), Cline daily (dryness 1/5) remain productive.
4. **Carry-forward from 2026-08-29**: 42 items (2026-08-29's #1 hook spawn cwd validation CRITICAL HIGH ~6h, 2026-08-29's #2 Langfuse minify detection CRITICAL MODERATE ~4h, 2026-08-28's #1 session history FTS HIGH ~10h, 2026-08-28's #2 remote MCP connect timeout MODERATE ~4h, 2026-08-28's #3 global rules path discovery MODERATE ~3h, 2026-08-27's #3 apply_patch CRLF preservation MODERATE ~5h, 2026-08-19's #1 streaming command output HIGH ~12h, 2026-08-15's #1 image generation HIGH ~8h, 2026-08-20's #1 file mention quoting MODERATE ~5h, 2026-08-21's #1 tool hook context injection MODERATE ~8h, plus 32 others). Agent 2 should surface 2026-08-29's #1 (hook spawn cwd validation), 2026-08-28's #2 (remote MCP connect timeout), 2026-08-28's #3 (global rules path discovery), 2026-08-28's #1 (session history FTS search), 2026-08-27's #3 (apply_patch CRLF preservation).
5. **Observation**: Sunday 2026-08-30 delivered zero new actionable items (all commits are carry-forwards from 2026-08-29 or earlier). Two critical carry-forwards remain top priority: hook spawn cwd validation (security fix for future hooks implementation) and Langfuse minify detection (defensive pattern for any telemetry integration). OpenCode dryness reached 5/5 threshold — propose demotion to weekly (Mon only) in next brief to reduce review overhead. Kilocode and Cline remain productive daily repos (both reset dryness to 1/5 today after yielding actionable items 2026-08-29). Engineering pipeline: zero open auto-implement issues, ready for Agent 2 to surface top priorities from 42-item carry-forward backlog.

(End of file)
