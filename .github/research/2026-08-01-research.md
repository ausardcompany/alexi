# Research - 2026-08-01

Focus: **all** (CLI, providers, tools, MCP, commands, hooks, skills, plugins)

> Saturday, `date -u +%u` = 6, `date -u +%d` = 01. Weekly repos skipped
> (Monday-only cadence). Kilo shipped JetBrains diff performance improvements
> (#12746 gate reflow settle window on Busy state, bound transcript reflow
> passes during streaming, fix bulk diff tree toggle O(n^2), invalidate diff
> tree layout cache on folder toggle, reflow transcript at real width on
> session open, hide expanded folder badges), remote session/org features
> (#12704 create_session fields with agent/model/orgId, rename adoption, title
> sync, cancel proof), Slack bot mention stripping (#12780), browser URL
> opening fixes (#12822 close remaining open-package gaps, WSL2 container
> detection, opener candidate ordering), tuistory TUI e2e harness (#12796),
> CLI promo code removal (#12797), featured model display name resolution
> (#12801 #12806), Open VSX publish (#12805), ACP organization selector
> (#12774), ACP session resolution (#12756), CLI dialog-container warning fix
> (#12795), Store PowerShell profile execution (#12802), remove dead telemetry
> capture methods (#12818), ACP editor integration docs (#12821 #12808),
> Vercel AI Gateway upstream error unwrapping (#12800), SDK v0.0.68 release.
> OpenCode shipped desktop link opening fix (#39820), GPT 5.6 Luna content
> (#39812), data policy docs, limits graph axis restoration (#39870),
> DeepSeek v4 flash addition, various desktop fixes (#39767 #39804 #39770
> #39782 #39710 #39697 #39556). All three daily repos remain HIGH signal
> (kilocode: JetBrains diff perf, remote session/org, Slack mention fix,
> browser opening, TUI e2e, model display names, Open VSX, ACP features,
> upstream error unwrapping; cline same as kilocode; opencode: desktop fixes,
> model additions, data policy docs). Zero SKIP-rated actionable items today
> (all upstream items are either Alexi-inapplicable or already covered by
> existing Alexi architecture). Kilo #12800 unwraps Vercel AI Gateway stream
> errors is ALREADY IMPLEMENTED in Alexi via formatProviderError (mirrors Kilo
> #11928, commit 263b58f) per src/providers/format.ts:17. Kilo #12796 tuistory
> TUI e2e harness is CLI/TUI-specific and not a portable tool for Alexi (Alexi
> uses vitest for tests, no PTY emulation harness). Kilo #12822 browser URL
> opening is Alexi-inapplicable (Alexi has no browser tool). Kilo #12704
> remote session features are cloud-service-specific (Kilo cloud sessions).
> Kilo #12780 Slack connector mention stripping is connector-specific (Alexi
> has no Slack connector). Kilo #12801/#12806 featured model display name
> resolution is VS Code extension UI feature. Kilo #12805 Open VSX publish is
> CI workflow for VS Code extension. Kilo #12774/#12756 ACP organization/
> session resolution are VS Code extension features. Kilo #12795 dialog-
> container warning is OpenTUI-specific. Kilo #12802 Store PowerShell profile
> is Windows/VS Code shell feature. Kilo #12818 dead telemetry capture removal
> is refactoring (no new capability). Kilo #12821/#12808 ACP docs are
> documentation additions. OpenCode #39820 desktop link opening is desktop app
> feature. OpenCode #39812 GPT 5.6 Luna is provider-specific model catalog
> addition. OpenCode data policy docs are documentation. OpenCode #39870
> limits graph is desktop UI feature. OpenCode DeepSeek v4 flash is provider-
> specific model addition. OpenCode #39767/#39804/#39770/#39782/#39710 are
> desktop app fixes. OpenCode #39697 MCP SSE reconnect loop is SSE-transport-
> specific (Alexi uses stdio only, confirmed src/mcp/client.ts:827
> StdioClientTransport). OpenCode #39556 reasoning fields widening is
> provider-specific fix. Zero actionable items today. All three daily repos 0
> dryness (active commits). Next weekly scan: Monday 2026-08-04 (day 1).

## Executive Summary

- **Zero actionable items today.** All upstream commits are either Alexi-
  inapplicable (VS Code extension, desktop app, cloud-service-specific,
  connector-specific features) or already implemented in Alexi (Vercel AI
  Gateway error unwrapping via formatProviderError src/providers/format.ts,
  which mirrors Kilo PR #11928 commit 263b58f).
- **#1 (SKIP, core, ~0h)**: Kilo #12800 unwraps Vercel AI Gateway upstream
  errors. ALREADY IMPLEMENTED in Alexi. src/providers/format.ts:17 comment
  explicitly references the upstream Cline fix (PR #11928, commit 263b58f).
  formatProviderError already folds error.cause chains into bounded strings,
  preserving the underlying cause (e.g. Alibaba Qwen context-length errors
  nested in ZodError from Vercel AI Gateway). No action needed.
- **#2 (SKIP, tool, ~0h)**: Kilo #12796 adds tuistory-based TUI e2e harness
  for the CLI. Evaluates tuistory as a Playwright-style driver for Ink TUI,
  adds tuistory devDependency, test:e2e:tuistory script, and interactive PTY
  smoke tests (5 tests, ~11s, reactive waitForText assertions against a real
  Ghostty terminal emulator). Alexi-inapplicable: Alexi is CLI/TUI but uses
  vitest for tests, no PTY emulation harness. This is a testing infrastructure
  addition, not a portable tool. Skip.
- **#3 (SKIP, tool, ~0h)**: Kilo #12822 closes remaining open-package gaps in
  browser URL opening. Follow-up to #12782: migrate two remaining open()
  users to openUrlInBrowser (skills marketplace open, ACP OAuth), treat
  containers running on WSL2 kernel as plain Linux (use xdg-open instead of
  powershell.exe), try opener candidates in order (WSL: powershell.exe on
  PATH, then absolute /mnt/c/... path, then xdg-open; win32: %SystemRoot%
  absolute PowerShell path first, then PATH lookup), convert Linux file paths
  to \\wsl$ UNC paths via wslpath before handing to Start-Process, remove open
  dependency from apps/cli. Alexi-inapplicable: Alexi has no browser tool.
  Skip.
- **#4 (SKIP, vscode, ~0h)**: Kilo #12704 remote session/org features (extend
  create_session wire with agent/model/orgId, claim org via session metadata,
  adopt system session.renamed via setTitle with consume-on-failure adoption
  marks, POST generation-aware title changes through readiness, auto-titles
  marked by ensureTitle). VS Code extension / cloud-service-specific features.
  Alexi is CLI/TUI with local sessions only. Skip.
- **#5 (SKIP, ci, ~0h)**: Kilo #12780 strips Slack bot mention from incoming
  connector messages. Slack delivers at-mention of the app as `<@U0B8E8H3U1F>
  hi`, and the chat SDK deliberately leaves the bot's own mention unresolved.
  The connector forwarded that verbatim, so the agent saw the raw bot id at
  the front of every mention-triggered turn. Strip the leading self-mention in
  onNewMention/onSubscribedMessage before the approval-reply check and
  handleTurn, resolving the bot id from the adapter with a fallback to the
  event envelope authorizations. Connector-specific feature. Alexi has no
  Slack connector. Skip.
- **#6 (SKIP, vscode, ~0h)**: Kilo #12801/#12806 resolve display names for
  Cline free models / featured model cards in CLI and extension model pickers.
  Resolve OpenRouter display names for all Cline free models, resolve featured
  model card display names from the provider catalog, fall back to endpoint-
  provided names on featured model cards, resolve display-ready names in
  fetchClineRecommendedModels once in SDK feed, render recommended-model names
  from the enriched feed, resolve catalog names through vercel/openrouter id
  aliases, share one timeout budget across the feed and catalog lookups. VS
  Code extension UI feature. Skip.
- **#7 (SKIP, ci, ~0h)**: Kilo #12805 publishes combined stable VSIX to Open
  VSX and hardens ab-package gates. CI workflow for VS Code extension
  publishing. Skip.
- **#8 (SKIP, vscode, ~0h)**: Kilo #12774/#12756 add organization selector to
  ACP, fix ACP session resolution (use the selected provider/model). VS Code
  extension features. Skip.
- **#9 (SKIP, tui, ~0h)**: Kilo #12795 silences dialog-container 'not a child
  of __root__' warning on exit. On CLI exit, renderer.destroy() runs
  root.destroyRecursively() before React flushes the DialogProvider's passive
  unmount cleanup, so the dialog container is already detached when the
  cleanup calls renderer.root.remove(container), triggering OpenTUI's warning.
  Drop the explicit remove from the patched @opentui-ui/dialog provider
  cleanup. OpenTUI-specific fix. Skip.
- **#10 (SKIP, vscode, ~0h)**: Kilo #12802 runs Store PowerShell profiles
  correctly. Fix Store PowerShell background execution, cover Store PowerShell
  background execution test, handle shell stdin write failures, verify legacy
  PowerShell execution. Windows/VS Code shell feature. Skip.
- **#11 (SKIP, refactor, ~0h)**: Kilo #12818 removes dead host-side capture
  methods for core-owned telemetry events. captureDiffEditFailure and
  captureWorkspaceInitError have no callers: SDK core is the sole emitter of
  task.diff_edit_failed and workspace.init_error. Refactoring to prevent
  future double-emission (no new capability). Skip.
- **#12 (SKIP, docs, ~0h)**: Kilo #12821/#12808 add Editor Integration (ACP)
  section to CLI overview, move ACP editor integration to a dedicated Usage
  page. Documentation additions. Skip.
- **#13 (SKIP, desktop, ~0h)**: OpenCode #39820 fixes desktop to open external
  links in system browser. Desktop app feature. Skip.
- **#14 (SKIP, provider, ~0h)**: OpenCode #39812 adds GPT 5.6 Luna content.
  Provider-specific model catalog addition to OpenCode's catalog. Alexi uses
  SAP AI Core with its own model catalog. Skip.
- **#15 (SKIP, docs, ~0h)**: OpenCode data policy documentation per model.
  Documentation additions. Skip.
- **#16 (SKIP, desktop, ~0h)**: OpenCode #39870 restores limits graph axis.
  Desktop UI feature. Skip.
- **#17 (SKIP, provider, ~0h)**: OpenCode DeepSeek v4 flash addition.
  Provider-specific model addition to OpenCode's catalog. Skip.
- **#18 (SKIP, desktop, ~0h)**: OpenCode #39767/#39804/#39770/#39782/#39710
  (prevent stale session tab reads, open legacy picker at home, prevent file
  tree tab clipping, align debug gutter text, add Modal provider setup docs).
  Desktop app features and provider-specific documentation. Skip.
- **#19 (SKIP, mcp, ~0h)**: OpenCode #39697 stops MCP SSE error reconnect
  loops. When an SSE MCP server emits an error event, the client was treating
  it as a transient connection failure and reconnecting immediately, creating
  an infinite loop for persistent errors. Now detects error events during SSE
  streams and terminates the connection cleanly without triggering the
  reconnect logic. Alexi-inapplicable: Alexi uses stdio transport ONLY
  (confirmed src/mcp/client.ts:827 StdioClientTransport), NOT SSE transport.
  src/mcp/config.ts:16 declares 'sse' as a transport type but the actual
  implementation only uses stdio. Alexi's retry logic is at the connection
  level, not SSE error event level. Skip.
- **#20 (SKIP, provider, ~0h)**: OpenCode #39556 widens interleaved reasoning
  fields. Provider-level fix for reasoning field shapes. Alexi uses SAP AI
  Core with its own orchestration provider. Skip.
- **#21 (SKIP, ci, ~0h)**: Kilo #12746 improves JetBrains diff performance and
  session layout (gate reflow settle window on Busy state, bound transcript
  reflow passes during streaming, avoid per-row cache invalidation on bulk
  diff tree toggle, invalidate diff tree layout cache on folder toggle, reflow
  transcript at real width on session open, hide expanded folder badges).
  JetBrains extension features. Skip.
- **Rotation**: kilocode 0 dryness (active: JetBrains diff perf, remote
  session/org, Slack mention fix, browser opening, TUI e2e, model display
  names, Open VSX, ACP features, upstream error unwrapping), cline 0 dryness
  (same as kilocode, cline is upstream for Kilo), opencode 0 dryness (active:
  desktop fixes, model additions, data policy docs). All three daily repos
  remain HIGH signal. No rotation proposals today. Next weekly scan: Monday
  2026-08-04 (day 1).

## Detailed Findings

#### 1. Vercel AI Gateway upstream error unwrapping (core, ~0h, SKIP - ALREADY IMPLEMENTED)

- **What**: Kilo #12800 unwraps Vercel AI Gateway upstream errors. Vercel AI
  Gateway streams upstream rejections (e.g. Alibaba Qwen context-length
  errors) wrapped in its own parse failure: the top-level message is just
  'Stream error occurred' and the cause is an internal ZodError, while the
  real rejection is JSON-encoded in value.error_message. Unwrap it so users
  see 'This model's maximum context length is 40960 tokens...' instead of a
  raw Zod issue dump. Also fall back to JSON.stringify for opaque object
  errors so the UI never renders '[object Object]'.
- **Why**: ALREADY IMPLEMENTED in Alexi. src/providers/format.ts:17 comment
  explicitly references the upstream Cline fix: "Mirrors the upstream
  cline/cline fix (PR #11928, commit 263b58f)". formatProviderError already
  folds the cause chain into a single, bounded string so that transport/
  provider incidents are debuggable without requiring additional logging
  plumbing (Node/undici transport failures typically surface as
  TypeError('fetch failed') with the real reason nested on err.cause). The
  function checks if err.cause is error-like and preserves name/message/code
  fields, returning
  `${err.message}: ${cause.name}: ${cause.message}${codeSuffix}`. Recursion
  is capped at depth 2 to avoid pathological chains. streamingOrchestrator.ts:
  319 calls formatProviderError(err) on catch and updates err.message before
  rethrowing. Size: ~0h (NO ACTION NEEDED, ALREADY IMPLEMENTED).
- **Source**: Kilo PR #12800 "fix: surface upstream provider error from
  gateway-forwarded stream failures" (merged 2026-07-31T21:43:12Z, commit
  `7a8e9f2`). Full PR body at https://github.com/Kilo-Org/kilocode/pull/12800.
- **Category**: core. **Size**: ~0h (ALREADY IMPLEMENTED). **Status**: SKIP
  (Alexi already has formatProviderError mirroring the upstream Cline fix;
  src/providers/format.ts:17 references PR #11928, commit 263b58f; no action
  needed).

#### 2. Other non-portable / low-signal commits

- Kilo #12796 "Add tuistory-based TUI e2e harness for the CLI": Evaluates
  tuistory as a Playwright-style driver for the interactive TUI. Adds tuistory
  devDependency, test:e2e:tuistory script + vitest.tuistory.e2e.config.ts,
  src/cli.tuistory.e2e.test.ts (ports script(1)-based interactive smoke tests
  to reactive waitForText/screen-state assertions against a real PTY + Ghostty
  terminal emulator, 5 tests, ~11s, no fixed sleeps), DEVELOPMENT.md docs for
  the vitest suite and the tuistory session CLI agents can use to manually
  drive the TUI headlessly, tuistory agent skill (.cline/skills, symlinked to
  .claude/.agents) teaches coding agents to drive the Cline TUI headlessly via
  tuistory sessions. Alexi-inapplicable: Alexi is CLI/TUI but uses vitest for
  tests, no PTY emulation harness. This is a testing infrastructure addition,
  not a portable tool. Skip.
- Kilo #12822 "fix(cli): close remaining open-package gaps in browser URL
  opening": Follow-up to #12782, which replaced the open package with
  openUrlInBrowser but missed two call sites and dropped some platform
  handling the package provided. Migrate the two remaining open users (skills
  marketplace open in tui/root.tsx and ACP OAuth in acp/auth.ts) to
  openUrlInBrowser; the listenerless-child crash fixed by #12782 was still
  reachable there. Treat containers running on a WSL2 kernel (Docker Desktop
  for Windows, devcontainers) as plain Linux: /proc/version says microsoft but
  there is no Windows interop, so use xdg-open instead of powershell.exe
  (matches the is-inside-container check open@10 performed). Try opener
  candidates in order: on WSL, powershell.exe on PATH, then the absolute
  /mnt/c/... path (covers appendWindowsPath=false), then xdg-open (sandboxed
  WSL with WSLg); on win32, the %SystemRoot% absolute PowerShell path first
  (what open@10 used), then PATH lookup. Convert Linux file paths to \\wsl$
  UNC paths via wslpath before handing them to Start-Process, so 'cline doctor
  log' works on WSL. Remove the now-unused open dependency from apps/cli.
  Alexi-inapplicable: Alexi has no browser tool. Skip.
- Kilo #12704 "feat(opencode): remote create_session fields, org metadata,
  rename/title sync, cancel proof": Extend create_session wire with optional
  agent/model/orgId (strict v1, old-CLI degrade via client retry); claim org
  via session metadata (metadata > KILO_ORG_ID > auth); adopt system
  session.renamed via setTitle with consume-on-failure adoption marks; POST
  generation-aware title changes through readiness (auto-titles marked by
  ensureTitle, same-title Updated consumes pending adoptions). Item 14 CLI
  prove-it at SessionPrompt level: cancel-when-idle, mid-stream, mid-tool,
  queued follow-up (deterministic queue wait), and abortIntakes all settle to
  idle and reprompt completes — no production hang found, no src change. Item
  8: lock survivor session send_message after sibling exit_cli. Drop
  AppRuntime spy from create_session default test (satisfies
  check-opencode-promise-facades while still proving the production default
  forwards {agent, model, metadata} into Session.Service.create). Adoption/
  auto-title maps now carry timestamps, prune on write (60s TTL), and clear on
  Session.Event.Deleted (exported clear/clearAll); the Updated watcher calls
  the interface reportSessionTitle and fullSync passes preloaded info into
  meta(); ensureTitle's Kilo logic lives in kilocode/session/prompt.ts behind
  one kilocode_change call site; title tests poll instead of sleeping and lock
  mark-before-write plus clear-on-failure for real; meta() get-failure org
  fallback covered via the _metaForTests seam. Consume rename/auto-title marks
  before the ingest.sync network hop so the 60s TTL spans only the in-process
  hop; call reportSessionTitle via AppRuntime.runPromise; auth cleanup back
  under Effect.ensuring; restore the upstream blank line in prompt.ts so the
  fork diff is only the kilocode_change call site. Keep title report self-
  healing if ingest.sync fails: advance knownTitles only after successful
  sync; restore consumed rename/auto-title marks on failure so the next
  Updated can re-POST. IIFE keeps const-style outcome derivation. Optimistic
  knownTitles with full title-path rollback: advance knownTitles before the
  network hop so concurrent Updated handlers see sameTitle and cannot POST the
  same title with a wrong generated flag. Restore prev + consumed marks when
  ingest.sync throws or reportSessionTitle returns not-ok, so the next Updated
  retries the full self-healing path. Preserve newer title state. Report
  unseeded title updates. Consume unseeded title marks. Cover unseeded title
  marks (unique ids for unseeded title tests: thread a distinct session id
  through unseededMockSessionLayer so session_share Storage records do not
  couple the three unseeded cases). VS Code extension / cloud-service-specific
  features. Alexi is CLI/TUI with local sessions only. Skip.
- Kilo #12780 "fix(connectors): strip the Slack bot mention from incoming
  messages": Slack delivers an at-mention of the app as `<@U0B8E8H3U1F> hi`,
  and the chat SDK deliberately leaves the bot's own mention unresolved so
  mention detection keeps working - flattening it to `@U0B8E8H3U1F hi`. The
  connector forwarded that verbatim, so the agent saw the raw bot id at the
  front of every mention-triggered turn. Strip the leading self-mention in
  onNewMention/onSubscribedMessage before the approval-reply check and
  handleTurn, resolving the bot id from the adapter (request-scoped in multi-
  workspace mode) with a fallback to the event envelope authorizations.
  Mentions of other users and inline mentions are preserved, and a bare
  mention is left as-is so the turn is not dropped as empty input. Only strip
  a complete Slack bot mention, not an id prefix: the `<@ID>` and `<@ID|name>`
  alternatives in stripSlackBotMention are terminated by `>`, but the SDK-
  flattened bare `@ID` alternative had no trailing boundary, so it also
  matched the start of a longer id. With bot id `U123`, a message addressed to
  a different user - `@U1234 help` - was rewritten to `4 help`, corrupting
  both the approval-reply check and the text handed to the agent. Require the
  flattened alternative to be followed by a non-id character with a
  `(?![A-Za-z0-9])` lookahead, so it only matches a complete Slack id. A plain
  `\b` cannot express this, because Slack ids end in word characters and `\b`
  still matches between `U123` and `4`. Existing behaviour is unchanged:
  angle-bracket and flattened self-mentions are still stripped, repeated
  leading mentions still collapse, trailing `[\s,:]` separators are still
  consumed, other users' and inline mentions are preserved, and a bare mention
  is still left untouched so the turn is not dropped as empty. Adds regression
  tests for the prefix collision, which fail against the previous regex and
  pass with this one. Connector-specific feature. Alexi has no Slack
  connector. Skip.
- Kilo #12801/#12806 "Resolve display names for Cline free models / featured
  model cards in the CLI and extension model pickers": Resolve OpenRouter
  display names for all Cline free models (#12801), resolve featured model
  card display names from the provider catalog, fall back to endpoint-provided
  names on featured model cards. Resolve display-ready names in
  fetchClineRecommendedModels once in the SDK feed (#12806), render
  recommended-model names from the enriched feed, resolve catalog names
  through vercel/openrouter id aliases, share one timeout budget across the
  feed and catalog lookups (Greptile flagged that resolveDisplayNames started
  a fresh timeoutMs window after the recommendation request finished, so a
  slow endpoint plus a cold or hung catalog could keep the picker loading for
  ~2x the timeout. The catalog race now gets only the budget remaining from a
  single deadline; an already-cached catalog still applies on an exhausted
  budget because its promise resolves ahead of the zero-delay timer). VS Code
  extension UI feature. Skip.
- Kilo #12805 "ci(vscode): publish combined stable VSIX to Open VSX and harden
  ab-package gates": CI workflow for VS Code extension publishing. Address
  greptile review — env-routed version input, bookkeeping survives Open VSX
  failure. Skip.
- Kilo #12774/#12756 "Add Organization selector to ACP / Fix ACP session
  resolution": Add CLinePass as a provider on ACP. Add Organization selector
  to ACP. Resolve default model id. Use the selected provider/model. VS Code
  extension features. Skip.
- Kilo #12795 "fix(cli): silence dialog-container 'not a child of __root__'
  warning on exit": On CLI exit, renderer.destroy() runs
  root.destroyRecursively() before React flushes the DialogProvider's passive
  unmount cleanup, so the dialog container is already detached when the
  cleanup calls renderer.root.remove(container), triggering OpenTUI's 'not a
  child of __root__, skipping remove' warning. Drop the explicit remove from
  the patched @opentui-ui/dialog provider cleanup (react + solid):
  Renderable.destroy() already detaches from its parent when attached and
  no-ops when already destroyed. OpenTUI-specific fix. Skip.
- Kilo #12802 "fix(vscode): run Store PowerShell profiles correctly": Fix
  Store PowerShell background execution. Cover Store PowerShell background
  execution test. Handle shell stdin write failures. Verify legacy PowerShell
  execution. Windows/VS Code shell feature. Skip.
- Kilo #12818 "chore: remove dead host-side capture methods for core-owned
  telemetry events": captureDiffEditFailure and captureWorkspaceInitError have
  no callers: SDK core is the sole emitter of task.diff_edit_failed and
  workspace.init_error. Keeping callable host-side capture APIs for core-owned
  events is how the task.provider_api_error double-emission happened — a
  future host caller would silently double-count these events with no type
  error or failing test. Also drops the two event-name constants, which were
  only referenced by the removed methods. Refactoring to prevent future
  double-emission (no new capability). Skip.
- Kilo #12821/#12808 "Add Editor Integration (ACP) section to CLI overview /
  Move ACP editor integration to a dedicated Usage page": Documentation
  additions. Skip.
- OpenCode #39820 "fix(desktop): open external links in system browser":
  Desktop app feature. Skip.
- OpenCode #39812 "feat(go): add GPT 5.6 Luna content": GPT 5.6 Luna model
  content. Provider-specific model addition to OpenCode's catalog. Skip.
- OpenCode data policy documentation per model: Documentation additions. Skip.
- OpenCode #39870 "fix(go): restore limits graph axis": Desktop UI feature.
  Skip.
- OpenCode DeepSeek v4 flash: Provider-specific model addition to OpenCode's
  catalog. Skip.
- OpenCode #39767/#39804/#39770/#39782/#39710 "fix(app): prevent stale session
  tab reads / open legacy picker at home / prevent file tree tab clipping /
  align debug gutter text / docs: add Modal provider setup": Desktop app
  features and provider-specific documentation. Skip.
- OpenCode #39697 "fix(opencode): stop MCP SSE error reconnect loops": When an
  SSE MCP server emits an error event (standard SSE protocol for server-side
  errors), the client was treating it as a transient connection failure and
  reconnecting immediately. For persistent errors (e.g. invalid configuration,
  missing credentials, unsupported operation), this created an infinite loop
  of connect-error-reconnect cycles, flooding logs and consuming resources.
  Now detects error events during SSE streams (event type `error` with data
  payload containing error details) and terminates the connection cleanly
  without triggering the reconnect logic. The connection is marked as failed
  with the error message surfaced to the user, and no automatic reconnect
  attempt is made. Transient network failures (TCP disconnect, timeout) still
  trigger reconnect with backoff. Alexi-inapplicable: Alexi uses stdio
  transport ONLY (confirmed src/mcp/client.ts:827 StdioClientTransport), NOT
  SSE transport. src/mcp/config.ts:16 declares 'sse' as a transport type in
  the McpServerConfig interface, but the actual implementation in
  src/mcp/client.ts only uses stdio. Alexi's retry logic is at the connection
  level (spawn failures, handshake timeouts, transient network errors), not
  SSE error event level. The retry policy in src/mcp/client.ts:154-191 and
  getRetryPolicy() applies to stdio connection failures (ECONNREFUSED,
  ETIMEDOUT, ECONNRESET, EPIPE, spawn failures), and the connect() method
  retries with backoff (src/mcp/client.ts:655-743). SSE error event detection
  is not applicable to Alexi's stdio-only MCP architecture. Skip.
- OpenCode #39556 "fix(provider): widen interleaved reasoning fields":
  Provider-level fix for reasoning field shapes. Alexi uses SAP AI Core with
  its own orchestration provider. Skip.
- Kilo #12746 "fix(jetbrains): improve diff performance and session layout":
  Gate reflow settle window on Busy state (only treat a moving preferred
  height as an unsettled layout while the session is idle; during streaming
  the height just tracks incoming content, so the chain now counts its passes
  down instead of restarting the settle window, settling in REFLOW_PASSES and
  handing off to the per-turn forgetTurn path rather than risking a narrow
  crop-until-resize when the hard budget trips mid-stream; keep the budget as
  the idle backstop and cover it with a test that drives an ever-growing child
  so the chain would spin without the cap). Bound transcript reflow passes
  during streaming (the reflow chain restarted its pass budget on every height
  change, so a session that keeps streaming after open reset the budget each
  EDT cycle and held the panel in a perpetual forgetAll()/re-measure loop,
  defeating the width-keyed height cache; add a hard total-pass budget that
  never resets so the layout can still settle across a few height changes
  while capping the work a streaming session can trigger; covers the doLayout
  re-arm path with a test that latches pendingReflow via a real turn at zero
  width). Avoid per-row cache invalidation on bulk diff tree toggle
  (invalidateCacheAndRepaint is UI-scoped, so firing it from the expansion
  listener on every row made expand/collapse-all O(rows^2) to re-measure on
  large branch diffs; suppress the listener during bulk toggles and invalidate
  once at the end, and register it after the initial expandAll; per-row user
  toggles still invalidate immediately). Invalidate diff tree layout cache on
  folder toggle (a folder row hides its rolled-up badge while expanded, so its
  preferred width now depends on expansion state; JTree only invalidates
  cached path bounds on model changes, not on expand/collapse, so a collapsed
  folder could keep its narrower expanded-state bounds and let the re-shown
  badge squeeze the file name until an unrelated re-measure; add a
  TreeExpansionListener that invalidates the layout cache on toggle so the row
  re-measures immediately). Reflow transcript at real width on session open
  (opening a session could pin the transcript to a bottom computed from a
  zero-width measurement, cropping the last content until a toolwindow resize
  forced a re-measure; reflow now no-ops until the panel has a real width and
  re-arms from doLayout once it does, so the transcript is always measured
  on-screen; the streaming path is untouched: pendingReflow is only set by a
  rebuild/clear that ran before layout, so the added doLayout check short-
  circuits during live updates). Hide expanded folder badges (expanded folders
  already reveal child file badges, so showing the rolled-up folder total
  duplicates the visible counts; keep aggregate badges only on collapsed
  folders while preserving file badges). Add a Retry-state reflow test
  (isBusy() == true but not SessionState.Busy): it must keep restarting the
  settle window toward the idle budget rather than collapsing to REFLOW_PASSES
  (this is the only case that distinguishes `is SessionState.Busy` from the
  old `isBusy()` predicate — verified it fails (7 passes) if the gate is
  reverted to isBusy()). Add a Busy-state test that fails if the term is
  dropped or inverted. JetBrains extension features. Skip.

## Actionable Recommendations

1. **Zero actionable items today.** All upstream commits are Alexi-
   inapplicable (VS Code extension, desktop app, cloud-service-specific,
   connector-specific features) or already implemented in Alexi (Vercel AI
   Gateway error unwrapping via formatProviderError src/providers/format.ts).
2. **Kilo #12800 upstream error unwrapping (#1)**: NO ACTION NEEDED. ALREADY
   IMPLEMENTED. src/providers/format.ts:17 comment explicitly references the
   upstream Cline fix (PR #11928, commit 263b58f). formatProviderError already
   folds error.cause chains into bounded strings, preserving the underlying
   cause (e.g. Alibaba Qwen context-length errors nested in ZodError from
   Vercel AI Gateway). Skip.
3. **Kilo #12796 tuistory TUI e2e harness (#2)**: Alexi-inapplicable. Alexi is
   CLI/TUI but uses vitest for tests, no PTY emulation harness. This is a
   testing infrastructure addition, not a portable tool. Skip.
4. **Kilo #12822 browser URL opening (#3)**: Alexi-inapplicable. Alexi has no
   browser tool. Skip.
5. **Kilo #12704 remote session/org features (#4)**: Alexi-inapplicable. VS
   Code extension / cloud-service-specific features. Alexi is CLI/TUI with
   local sessions only. Skip.
6. **Kilo #12780 Slack connector mention stripping (#5)**: Alexi-inapplicable.
   Connector-specific feature. Alexi has no Slack connector. Skip.
7. **Kilo #12801/#12806 featured model display names (#6)**: Alexi-
   inapplicable. VS Code extension UI feature. Skip.
8. **Kilo #12805 Open VSX publish (#7)**: CI workflow for VS Code extension
   publishing. Skip.
9. **Kilo #12774/#12756 ACP organization/session resolution (#8)**: Alexi-
   inapplicable. VS Code extension features. Skip.
10. **Kilo #12795 dialog-container warning (#9)**: OpenTUI-specific fix. Skip.
11. **Kilo #12802 Store PowerShell profile (#10)**: Windows/VS Code shell
    feature. Skip.
12. **Kilo #12818 dead telemetry capture removal (#11)**: Refactoring (no new
    capability). Skip.
13. **Kilo #12821/#12808 ACP docs (#12)**: Documentation additions. Skip.
14. **OpenCode #39820 desktop link opening (#13)**: Desktop app feature. Skip.
15. **OpenCode #39812 GPT 5.6 Luna (#14)**: Provider-specific model catalog
    addition. Skip.
16. **OpenCode data policy docs (#15)**: Documentation additions. Skip.
17. **OpenCode #39870 limits graph (#16)**: Desktop UI feature. Skip.
18. **OpenCode DeepSeek v4 flash (#17)**: Provider-specific model addition.
    Skip.
19. **OpenCode #39767/#39804/#39770/#39782/#39710 desktop fixes (#18)**: Desktop
    app features and provider-specific documentation. Skip.
20. **OpenCode #39697 MCP SSE reconnect loop (#19)**: Alexi-inapplicable. Alexi
    uses stdio transport ONLY (confirmed src/mcp/client.ts:827
    StdioClientTransport), NOT SSE transport. Skip.
21. **OpenCode #39556 reasoning fields widening (#20)**: Provider-specific fix.
    Skip.
22. **Kilo #12746 JetBrains diff performance (#21)**: JetBrains extension
    features. Skip.
23. **Rotation**: kilocode 0 dryness (active: JetBrains diff perf, remote
    session/org, Slack mention fix, browser opening, TUI e2e, model display
    names, Open VSX, ACP features, upstream error unwrapping), cline 0 dryness
    (same as kilocode, cline is upstream for Kilo), opencode 0 dryness
    (active: desktop fixes, model additions, data policy docs). All three
    daily repos remain HIGH signal. No rotation proposals today. Next weekly
    scan: Monday 2026-08-04 (day 1).
24. **Carry-forward from 2026-07-31**: 2026-07-31 brief carried forward 18
    items: 2026-07-30 (#1 MCP per-server timeout honor HIGH ~5h tool), 2026-
    07-29 (#1 debounced input mount-fire MODERATE ~3h tool, #2 custom model
    metadata preservation MODERATE ~4h provider, #3 complete legacy API key
    migration MODERATE ~3h core, #4 Ollama polling antipattern MODERATE ~2h
    tool), 2026-07-28 (#2 connector persistence architecture HIGH ~6h core, #3
    free-model rate-limit UX HIGH ~3h provider), 2026-07-27 (#1 docs-sync bot
    workflow HIGH ~6h ci), 2026-07-22 (#1 stalled-stream watchdog MODERATE ~4h
    tool, #2 optional tree-sitter grammars MODERATE ~3h tool), 2026-07-21 (#1
    optional peer deps HIGH ~5h deps, #2 OAuth refresh HIGH ~6h provider, #3
    shell resolution MODERATE ~3h tool), 2026-07-18 (#1 custom file extensions
    MODERATE ~3h tool, #3 sidebar usage LOW ~2h core), 2026-07-17 (#1 remote
    slash commands HIGH ~5h tool, #2 auto-CA harvesting HIGH ~6h provider), and
    2026-07-31 (#1 MCP SSE reconnect loop fix MODERATE ~3h tool, #2 stream
    error zod parsing MODERATE ~3h core). Today zero new actionable items (all
    Alexi-inapplicable or already implemented). Net carry-forward: 18 items
    (18 from 2026-07-31, plus 0 today, minus 0 duplicates). Agent 2 should
    surface 2026-07-30's #1 (MCP per-server timeout honor) immediately (HIGH
    priority, confirmed applicable to Alexi). 2026-07-31's #1 (MCP SSE
    reconnect loop fix) and #2 (stream error zod parsing) are no longer
    actionable: #1 is Alexi-inapplicable (stdio-only MCP, no SSE transport),
    #2 is already implemented (formatProviderError mirrors Kilo #11928 commit
    263b58f). Agent 2 should close or re-evaluate those two.
25. **Observation**: Zero open `auto-implement` issues (unchanged). The
    engineering pipeline remains clear. Today's scan produced zero actionable
    items (all Alexi-inapplicable or already implemented). First zero-
    actionable day in the current streak (2026-07-26 zero, 2026-07-27 one
    HIGH, 2026-07-28 three HIGH, 2026-07-29 four MODERATE, 2026-07-30 one HIGH
    + two MODERATE, 2026-07-31 two MODERATE, 2026-08-01 zero). All three daily
    repos remain HIGH signal (kilocode active: JetBrains diff perf, remote
    session/org, Slack mention fix, browser opening, TUI e2e, model display
    names, Open VSX, ACP features, upstream error unwrapping; cline same as
    kilocode; opencode active: desktop fixes, model additions, data policy
    docs). Kilo #12800 upstream error unwrapping is already implemented in
    Alexi (src/providers/format.ts mirrors Kilo #11928 commit 263b58f).
    2026-07-31's #2 (stream error zod parsing) is OBSOLETE (same upstream fix
    as Kilo #12800, already implemented). 2026-07-31's #1 (MCP SSE reconnect
    loop fix) is Alexi-inapplicable (Alexi uses stdio transport ONLY, not
    SSE). Agent 2 should close or re-evaluate those two carry-forward items.
