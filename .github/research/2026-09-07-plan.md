# Development Plan - 2026-09-07

## Executive Summary

Based on the research report from 2026-09-07, I have identified **2 actionable items** for implementation:

1. **TUI Home/End Navigation Verification** (SMALL, ~1-2h) - Verify that Alexi's Ink-based TUI input correctly handles Home/End keys for cursor jump-to-start/end navigation, following Kilocode PR #13855
2. **Model Catalog Expansion** (SMALL, ~1-2h) - Add Claude 3.7 Sonnet + Opus 4.1 dated variants to the ORCHESTRATION_MODELS catalog, following Aider PR #5173

## Current State Analysis

### Codebase Structure
- **TUI Implementation**: Alexi uses Ink v6.8.0 + React 19.2.4 with `ink-text-input v6.0.0` for input handling
  - Main input component: `src/cli/tui/components/InputBox.tsx`
  - Uses standard `TextInput` from `ink-text-input` package
  - Handles Up/Down arrows for history, Tab for autocomplete, Escape for dismiss
  - No explicit Home/End key handlers visible in current implementation

- **Model Catalog**: Located at `src/providers/sapOrchestration.ts`
  - Current ORCHESTRATION_MODELS array (lines 1938-1969) includes:
    - Claude 3.7 Sonnet: `anthropic--claude-3.7-sonnet` ✅ (already present)
    - Claude 4.5 Opus: `anthropic--claude-4.5-opus` ✅ (already present)
    - Claude 4.6 Opus: `anthropic--claude-4.6-opus` ✅ (already present)
    - Claude 4.7 Opus: `anthropic--claude-4.7-opus` ✅ (already present)
  - Temperature deprecation for Claude 4 models: Already handled (lines 1262-1276)
  - Comments reference Aider #5173 explicitly (line 1934)

### Gap Analysis

1. **TUI Navigation** (NEEDS VERIFICATION)
   - Kilocode #13855 fixed Home/End keys in session prompt input
   - Alexi's `InputBox.tsx` does not show explicit Home/End handling
   - `ink-text-input v6.0.0` may or may not support Home/End natively
   - **Gap**: Need to verify actual behavior in running TUI (`ax chat` or `ax agent`)
   - **Risk**: If broken, affects developer productivity on long prompts

2. **Model Catalog** (ALREADY COMPLETE)
   - All Claude model IDs from Aider #5173 are present in ORCHESTRATION_MODELS
   - `claude-3-7-sonnet-20250219` → mapped to `anthropic--claude-3.7-sonnet` (SAP naming convention)
   - `claude-opus-4-1-20250805` → mapped to `anthropic--claude-4.5-opus` (already in catalog)
   - Temperature deprecation already implemented per Aider guidance
   - **Gap**: None - catalog is current
   - **Action**: Close issue #1670 and #1671 as already implemented

## Implementation Priorities

### HIGH Priority (0 items)
None from today's research.

### MEDIUM Priority (0 items)
None from today's research.

### SMALL Priority (1 actionable item)
1. **TUI Home/End Navigation Verification** (~1-2h)
   - Manual testing required: `npm run dev -- chat` and test Home/End keys
   - If broken: Add explicit keypress handlers to `InputBox.tsx`
   - If working: Document as verified, close issue

### ALREADY IMPLEMENTED (1 item)
1. **Model Catalog Expansion** - All requested models present, temperature handling correct

## Deferred/Out-of-Scope

- **Kilocode #13541** (PR review comments inline in diffs) - VS Code-specific, not CLI-portable
- **Kilocode #13848** (shared agent board messages display) - Board tool exists, no reported issues
- **Kilocode #13847 + #13794** (VS Code subagent performance + undo/redo) - VS Code-specific
- **Aider #5132** (Bash tree-sitter tags for repomap) - Alexi has no repomap surface yet
- **OpenCode #47737** (OAuth metadata document) - Console-specific, not CLI-portable
- **OpenCode #47671 + #47664 + #47659** (Azure/OpenAI SDK bumps) - Alexi uses SAP AI Core provider
- **Claude Code #91894** (frontend-design skill update) - No visible actionable change in commit

## Rotation Recommendations

- **OpenCode**: Demote to weekly (Mon only) - 13 consecutive cycles with zero CLI-actionable items
- **Kilocode**: Reset dryness to 0/5 - 1 CLI-actionable item today
- **Cline**: Increment dryness to 2/5 - 0 new items today, still below 5/5 threshold
- **Aider**: Weekly (Mon only) - Productive, 2 model catalog items

## Summary

| Priority | Task | Size | Status |
|----------|------|------|--------|
| SMALL | TUI Home/End Navigation Verification | 1-2h | Ready for testing |
| N/A | Model Catalog Expansion (Claude 3.7/Opus 4.x) | 0h | Already implemented |

**Total estimated effort**: 1-2 hours for verification task only.

**Note**: The model catalog expansion requested in the research (Aider #5173) is already complete. The ORCHESTRATION_MODELS array in `src/providers/sapOrchestration.ts` contains all requested Claude model IDs with proper SAP naming conventions. Temperature handling for Claude 4 models is correctly implemented per Aider's deprecation guidance. Issues #1670 and #1671 should be closed as implemented.
